# Phase 1-2: Foundation & Converter Core

[Return to Parent Plan](../evidence-foundry-buildout-v1.md)

**Column conventions** (apply to every task table below): `Estimate` is story points, never Effort.
`Model` values: `sonnet` \| `haiku`. `Effort` values (claude only): `adaptive` \| `extended`. See
`.claude/skills/planning/references/multi-model-guidance.md`. Gate rows (`task-completion-validator`,
`karen`) carry `Estimate: —` — they are reviewer checkpoints, not pointed build work, per decisions
block §6 H6 ("hidden plumbing... already inside phase numbers; do not add on top").

---

## Phase 1: Foundation & Fixtures

**Duration**: ~2-3 engineer-days
**Dependencies**: None — first phase in the plan
**Assigned Subagent(s)**: general-purpose executor (sonnet); task-completion-validator gate
**Exit gate** (decisions block §3): `npm run check` green; fixture loads; validator rejects a
seeded-bad KB.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|---------------|
| P1-T1 | Path-mapping worknote (blocking) | Per PRD FR-5 / decisions block §2: write `.claude/worknotes/evidence-foundry-buildout/path-mapping.md` reconciling every `02-evidence-foundry-on-research-foundry.md` path reference (`data/rules.json`, `data/evidence.json`, `data/candidates.json`, `src/evidence.js` as sole registry) to its current-tree equivalent (`modules/anemia/{rules,candidates,evidence}.json`; `src/evidence.js` retained but unified per P1-T4). Confirm the `npm test` glob (`tests/*.test.mjs`) is unaffected by any planned change (OQ-5). This is a hard blocker — no Phase 2+ task may start before this lands. | Worknote committed; every stale-path row from PRD §2's table is present with its current-tree equivalent; explicit confirmation line that `npm test`'s glob is untouched by this feature | 0.5 | general-purpose | sonnet | adaptive | None |
| P1-T2 | `modules/cbc_suite_v1/module.json` envelope | Per FR-1 / `02 §3.2`, `02 §7.2` item 1: create `modules/cbc_suite_v1/module.json` matching `modules/anemia/module.json`'s unsigned-stub shape (`id: "cbc_suite_v1"`, `title`, `schemaVersion`, `status: "unsigned-stub"`, `knowledgeBaseVersion`, `evidenceReviewedThrough`, `engineLabel`, `supportedAgeMonths`, `clinicalContentHash: null`, `approvedBy: []`, `validationRunId: null`, `supersedes: null`, `releasedAt: null`) plus the `02 §3.2` module-variable-envelope fields (`module_topic`, `intended_hcp_users`, `patient_population`, `intended_output`, `explicit_exclusions`, `jurisdictions`, `integration_targets`, `evidence_policy`) sourced verbatim from `02 §3.2`'s worked example. Field-presence validation only for the new envelope fields — no new JSON Schema file (decisions block §11 OQ-7 ruling names exactly 4 new schema types; this is not one of them). | `module.json` parses; both the unsigned-stub shape and all 8 envelope fields are present; `scripts/validate-kb.mjs` (post P1-T5) checks field presence on the envelope block, not just the stub shape | 0.75 | general-purpose | sonnet | adaptive | P1-T1 |
| P1-T3 | `modules/cbc_suite_v1/` package scaffold + registry wiring (OQ-1) | Implements this plan's OQ-1 resolution verbatim: create `modules/cbc_suite_v1/index.js` whose `deriveFacts`/`summarize`/`limitations` delegate directly to `modules/anemia/facts.anemia.js` (import and call, not duplicate); `modules/cbc_suite_v1/reference-ranges.json` as a byte-identical copy of `modules/anemia/reference-ranges.json`; `modules/cbc_suite_v1/rules.json` (`[]`), `modules/cbc_suite_v1/candidates.json` (`{}`), `modules/cbc_suite_v1/evidence.json` (`{sources: []}`) as empty-but-valid scaffolds. Register `cbc_suite_v1` in `src/modules/registry.js` (`getModule`/`listModules`, and additively in `MODULE_IDS`) and `src/facts/registry.js` (map `'cbc_suite_v1'` to the same delegated function). Do **not** register `cbc_suite_v1` in `src/ranges/registry.js` (OQ-1: ranges resolve through the delegated `anemia` call). `DEFAULT_MODULE_ID` stays `'anemia'`; update the existing tripwire comment in `src/modules/registry.js` to record why `'anemia'` remains correct now that a second module exists (no client-selectable moduleId surface is added). | `getModule('cbc_suite_v1')` resolves; `MODULE_IDS` includes both ids; `DEFAULT_MODULE_ID === 'anemia'` still passes `tests/module-registry.test.mjs`'s existing tripwire assertion; the tripwire comment is updated, not silently left stale; `deriveFacts(input, 'cbc_suite_v1')` produces output identical to `deriveFacts(input, 'anemia')` for the same input | 1.0 | general-purpose | sonnet | adaptive | P1-T2 |
| P1-T4 | Evidence-registry unification | Per FR-2 / `02 §4.19`: eliminate the `src/evidence.js` / `modules/anemia/evidence.json` dual-source-of-truth (PRD §8.3 risk). Generate `src/evidence.js`'s `KNOWLEDGE_BASE_VERSION`/`REVIEWED_THROUGH`/`EVIDENCE` exports from `modules/anemia/evidence.json` at build/import time (or replace both with a single injected immutable registry consumed identically in browser and server modes) — either satisfies FR-2; do not merely extend the existing partial drift check in `scripts/validate-kb.mjs` (that check already exists and only catches divergence after the fact, it does not eliminate the second hand-maintained source). | `src/evidence.js` no longer hand-maintains evidence content independently of `modules/anemia/evidence.json`; existing callers of `src/evidence.js` (`scripts/validate-kb.mjs`, `src/app.js`, `server.mjs`) need zero edits; `npm run check` stays green | 1.0 | general-purpose | sonnet | adaptive | P1-T3 |
| P1-T5 | Real JSON-Schema validation in `scripts/validate-kb.mjs` | Per FR-3: add actual JSON Schema validation of every module's `rules.json` against `schemas/rule.schema.json` (the current validator checks IDs/evidence-refs/candidate-refs/condition-evaluability structurally, but never runs the schema itself — confirmed by reading the script). Use a pinned JSON Schema validator dependency (draft 2020-12, matching `schemas/rule.schema.json`'s `$schema`). Seed one intentionally-invalid rule (extra property, violating `additionalProperties: false`) as a fixture under `tests/fixtures/` and assert `npm run validate` fails on it. | A seeded-bad rule (extra property) fails `npm run validate` with a specific schema-violation message; all 91 existing `modules/anemia/rules.json` entries and the empty `modules/cbc_suite_v1/rules.json` still pass | 0.75 | general-purpose | sonnet | adaptive | P1-T4 |
| P1-T6 | Sanitized fixture bundle (OQ-2) | Per FR-4, this plan's OQ-2 resolution: derive one sanitized, in-repo fixture bundle from the `RF-CBC-001` verified `rf` run (not `REG-001`/`REG-004` — both are legal-review-flagged and excluded from clinical-rule fixtures per PRD §8). Default to the rights-restricted fallback (`02 §4.10`): immutable passage hash + precise selector, not full text, unless the specific passages needed for the 4 slice rules are positively confirmed rights-clear. Write a hash-provenance note under `tests/fixtures/` recording the source `run_id`, bundle SHA-256, and the content-rights disposition of every included passage. | Fixture bundle committed under `tests/fixtures/`; hash-provenance note names `RF-CBC-001` explicitly and states the rights disposition per passage; fixture is loadable by P2's read-only bundle loader once it exists | 0.75 | general-purpose | sonnet | adaptive | P1-T1 |
| P1-T7 | `.gitignore` — `build/` (OQ-6) | Per decisions block §11 OQ-6 ruling: add `build/` to `.gitignore`. `build/kb-pack/` converter output is generated, never committed; committed golden/fixture outputs for converter tests live under `tests/fixtures/` (P1-T6, P2-T8, P5) instead. | `.gitignore` contains a `build/` entry; `git status` after a local `propose` run (once P3 exists) shows `build/kb-pack/` as ignored, not untracked | 0.25 | general-purpose | sonnet | adaptive | None |
| P1-GATE | `task-completion-validator` gate | Verify Phase 1 exit gate: `npm run check` green; `getModule('cbc_suite_v1')` resolves; seeded-bad-KB fixture fails `npm run validate`; fixture bundle loads. | All 4 exit-gate criteria pass; recorded in phase progress note | — | task-completion-validator | sonnet | adaptive | P1-T1..T7 |

**Phase 1 Quality Gates:**
- [ ] `npm run check` green
- [ ] `cbc_suite_v1` fixture/module loads via `getModule('cbc_suite_v1')`
- [ ] Seeded-bad-KB fixture fails `npm run validate` with a specific schema error
- [ ] `src/evidence.js` no longer independently hand-maintains evidence content
- [ ] Path-mapping worknote exists and is referenced by this plan (done — see parent §"Decisions & OQ Resolutions")

---

## Phase 2: Converter Core (EF-WP0)

**Duration**: ~4-5 engineer-days
**Dependencies**: Phase 1 complete
**Assigned Subagent(s)**: backend-architect (design), general-purpose executor (build); task-completion-validator gate; **karen milestone review** (decisions block §4 — converter core is the first of 3 named milestones)
**Integration Owner**: backend-architect — owns the Phase 2 design (P2-T1) and signs off that every build task's output satisfies the seam-invariant contract the design specifies.
**Seam Task** (R-P3): P2-T8 — the 15-invariant executable test suite is the explicit join point proving P2-T1's design and P2-T2..T7's builds agree.
**Exit gate** (decisions block §3): 15 seam invariants (`02 §2.3`) each covered by ≥1 executable test; converter refuses a non-`verified` bundle.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|---------------|
| P2-T1 | Converter CLI scaffold + design (seam task owner) | Per FR-6, `02 §4.1`, `02 §4.5`: design and scaffold `tools/rf-bundle-to-kb-pack/` as a Node.js ESM CLI (pinned YAML parser + JSON Schema validator deps, matching `02 §4.1`'s "Runtime" row) with verb dispatch for `inspect`/`verify` (this phase) and a `propose` stub (wired in P3). Define the internal module boundary (loader / hashing / eligibility / error-taxonomy / verb-handlers) that P2-T2..T7 build against, and document it as a short design note at `tools/rf-bundle-to-kb-pack/README.md`. This is the design task R-P3 requires an integration owner for. | `node tools/rf-bundle-to-kb-pack/cli.mjs --help` lists `inspect`/`verify`/`propose` (propose exits "not yet implemented" until P3); README documents the module boundary every subsequent task in this phase and Phase 3 builds against | 1.5 | backend-architect | sonnet | extended | P1-GATE |
| P2-T2 | Read-only bundle loader + artifact resolution | Per FR-6, `02 §4.3`: resolve `runs/<RUN>/evidence_bundle.yaml`, `claims/claim_ledger.yaml`, `reviews/verification.yaml`, `sources/src_*.md`, `extractions/ext_*.yaml` relative to `evidence_bundle.yaml.artifacts` (never hard-coded assumptions, per `02 §4.3`'s closing line), plus `modules/cbc_suite_v1/module.json` and `modules/cbc_suite_v1/authoring-decisions.yaml` (the latter doesn't exist until P3-T1 — loader must fail closed with a specific "decisions file not found" error, not a generic crash, when absent). Loader never writes to the run directory (seam invariant 6). | Loader resolves every artifact listed in the P1-T6 fixture's `evidence_bundle.yaml.artifacts`; a missing `authoring-decisions.yaml` produces the specific fail-closed error, not a stack trace; a test asserts the run directory's file mtimes/permissions are unchanged after a full loader pass | 1.0 | general-purpose | sonnet | adaptive | P2-T1 |
| P2-T3 | Hash pinning ("Pin" phase) | Per FR-7, `02 §4.6` Phase 1, seam invariant 5: SHA-256 every input artifact — `run_id`, bundle ID, bundle bytes, claim-ledger bytes, every referenced source-card's bytes — before any transformation step. Fail closed on a missing artifact, path escape outside the run directory, or a hash mismatch against `evidence_bundle.yaml`'s recorded values. | Every artifact in the P1-T6 fixture is hashed; a test that mutates one byte of a source card after pinning and re-runs the pin step detects the drift and fails closed; a path-escape attempt (`../` in an artifact path) is rejected | 1.0 | general-purpose | sonnet | extended | P2-T2 |
| P2-T4 | Converter-eligibility + status-reconciliation checks | Per FR-9, `02 §2.3` invariants 1/3/4, `02 §3.7`: reject (non-zero exit, no partial output) any bundle whose `evidence_bundle.yaml.status` is not exactly `verified`; reject any disagreement between the recorded process exit code and `reviews/verification.yaml.exit_code`/`passed`. Apply the `02 §3.7` converter-eligibility field table (source_card_id, evidence_id, locator, exact passage, source status, recency, applicability, laboratory context, threshold portability, conflicts, claim status, lifecycle) as pre-flight checks before any downstream phase runs. | A seeded non-`verified`-status fixture produces a non-zero exit and zero output files; a seeded exit-code/artifact-status mismatch fixture is rejected with a specific "process/artifact status disagreement" error, not silently passed through | 1.0 | general-purpose | sonnet | extended | P2-T2 |
| P2-T5 | Fail-closed error taxonomy (`rf` exit-code mapping) | Per FR-11, `02 §5.2`: map converter-internal failure states onto the `rf verify`/`rf council` exit-code taxonomy (0 ok · 1 usage · 2 schema · 3 governance · 4 unsupported · 5 budget · 6 adapter · 7 human-review) — never bypass exit 3 (governance) or exit 7 (human-review) as if they were ordinary failures; both must halt and surface distinctly from a generic error. | Each of the 8 exit codes has a distinct, named error path in the converter's error module; a test asserts exit 3 and exit 7 specifically do not get caught by the converter's generic-error handler | 0.75 | general-purpose | sonnet | adaptive | P2-T1 |
| P2-T6 | `inspect` verb | Per `02 §4.5`: `inspect --run-dir <dir> --module <module.json path>` runs the loader + hash-pinning + eligibility checks (P2-T2..T4) and prints a structured summary (artifact list, hashes, eligibility pass/fail per claim) without emitting any pack output. Zero network calls, zero LLM/generative-model invocations (FR-10). | `inspect` run against the P1-T6 fixture prints a structured, non-empty summary; a test asserts zero outbound network calls and zero calls to any model-invocation hook during `inspect` | 0.5 | general-purpose | sonnet | adaptive | P2-T3, P2-T4, P2-T5 |
| P2-T7 | `verify` verb (structural pre-check) | Per `02 §4.5`: `verify --pack <dir> --rule-schema schemas/rule.schema.json` validates a staged pack's structural shape (this phase: validates the *input*-side pre-checks only, since no pack exists to verify until P3's `propose` runs — implement against P2-T2..T4's loader output and leave the pack-output validation path as an explicit stub P5-T1 completes once `release-manifest.unsigned.json` exists). | `verify` exits 0 against a structurally sound fixture and non-zero against a seeded-malformed one; the pack-output-validation stub is explicitly marked (not silently incomplete) for P5 to finish | 0.5 | general-purpose | sonnet | adaptive | P2-T6 |
| P2-T8 | 15 seam-invariant test suite (seam task) | Per FR-8, `02 §2.3` (the central Phase 2 risk hotspot per decisions block §5): one executable test per invariant, at `tests/ef-converter-invariants.test.mjs` (flat, `ef-`-prefixed, matches the `tests/*.test.mjs` glob per OQ-5's pattern extended to converter-level tests — confirmed in P1-T1's worknote). Invariants 1-6 and 13-15 are covered by P2-T2..T7's own tests already; this task adds executable coverage for invariants 7-12 specifically (claim-eligibility routing by status, no confidence-to-probability translation, no absence-as-normal inference) using stub claim fixtures, since Phase 3 hasn't built real claim-mapping yet — and asserts, across the whole suite, that zero network calls and zero LLM/generative-model calls occur in any verb. | All 15 invariants from `02 §2.3` have ≥1 passing named test (15/15, not "most" — verified by a test-name-to-invariant-number cross-check in the file's header comment); zero-network/zero-LLM assertion runs across `inspect`, `verify`, and the P3 `propose` stub | 1.75 | general-purpose, testing specialist | sonnet | extended | P2-T5, P2-T6, P2-T7 |
| P2-GATE1 | `task-completion-validator` gate | Verify Phase 2 exit gate: 15/15 invariant tests pass; non-`verified` bundle refused; `inspect`/`verify` execute against the fixture. **Scope note**: this gate validates P2-T1..T8's build output only — `karen` milestone sign-off (P2-GATE2) is a separate, subsequent gate and is *not* in P2-GATE1's own acceptance scope; its absence at P2-GATE1 time is expected, not a P2-GATE1 finding. | All exit-gate criteria pass | — | task-completion-validator | sonnet | adaptive | P2-T1..T8 |
| P2-GATE2 | `karen` milestone review — converter core | Independently re-check the 15/15 invariant claim, the fail-closed exit-code taxonomy, and CLAUDE.md hard guardrails ("no generative model in the decision path," "no invented thresholds") against Phase 2's actual diff, not the plan's description of it. Runs only after P2-GATE1 has passed. | `karen` sign-off recorded; any gap becomes a new task before Phase 3 opens | — | karen | sonnet | adaptive | P2-GATE1 |

**Phase 2 Quality Gates:**

*Validated at P2-GATE1 (task-completion-validator; build-output scope):*
- [ ] All 15 seam invariants (`02 §2.3`) have ≥1 passing named test
- [ ] A seeded non-`verified` bundle produces a non-zero exit and zero output files
- [ ] Zero network calls and zero generative-model calls occur in any verb (test-enforced)
- [ ] `runs/<RUN>/` is never mutated by any verb (test-enforced)

*Validated at P2-GATE2 (`karen` milestone review; runs after P2-GATE1 passes — out of P2-GATE1's own scope):*
- [ ] `karen` milestone sign-off recorded

[Return to Parent Plan](../evidence-foundry-buildout-v1.md)
