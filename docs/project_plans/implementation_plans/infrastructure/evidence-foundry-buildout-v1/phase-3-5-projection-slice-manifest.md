# Phase 3-5: Projection, Slice & Manifest

[Return to Parent Plan](../evidence-foundry-buildout-v1.md)

**Column conventions**: same as Phase 1-2 file — `Estimate` in points, `Model`/`Effort` per the
Canonical Effort Vocabulary, gate rows carry `Estimate: —`.

---

## Phase 3: Projection & Drafting

**Duration**: ~4-5 engineer-days
**Dependencies**: Phase 2 complete (Phase 6 opens in parallel with this phase, per the parent plan's
wave assignment — no file overlap)
**Assigned Subagent(s)**: general-purpose executor (sonnet); task-completion-validator gate
**Exit gate** (decisions block §3): `propose` on the fixture yields a schema-valid pack; `mixed`/
`contradicted` claims emit conflict-visible objects, never one-sided rules.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|---------------|
| P3-T1 | Author `authoring-decisions.yaml` for the 4 slice rules | Per FR-14, `02 §4.12`: since the converter must not infer clinical Boolean logic from prose (FR-14, `02 §4.5`), hand-author `modules/cbc_suite_v1/authoring-decisions.yaml` with one `dec_*`-ID record per slice rule (young-infant abstention, local-range precedence, iron-deficiency pattern, marrow-red-flag) matching the `02 §4.12` schema (`decision_id`, `module_id`, `status: approved_for_rule_draft`, `basis.kind`, `basis.rf_claim_ids`, `basis.exact_assertion_ids`, `basis.reasoning`, `conflicts`, `clinical_effect`, `review` — all 4 `review.*` fields set `pending`, since no named clinical review occurs in E0). Cite the specific `RF-CBC-001` claim IDs from the P1-T6 fixture each decision is grounded in. | 4 decision records exist, one per slice rule; each cites ≥1 real claim ID from the fixture's `claim_ledger.yaml`; all `review.*` fields are `pending` (never silently marked approved); schema-valid per P3-T3's new `authoring-decisions.schema.json` | 1.5 | general-purpose | sonnet | adaptive | P2-GATE1 |
| P3-T2 | `evidence.json` enrichment | Per FR-12 (first half), `02 §4.9`: project the fixture's source cards into `modules/cbc_suite_v1/evidence.json` per the `02 §4.9` field-mapping table (`rfSourceCardId`, compatibility-alias `id`, `title`, `priority` via the controlled source-type/rank table, `publicationDate`/`year`, `authors`, `organization`, `url`/`doi`, `journal` — never synthesized, `limitations`, `conflictsWith`, `contentRights`, `supports[]` linked to assertion IDs, `reviewBy`/`surveillanceQuery`/`supersessionStatus`, `recencyNote` when applicable). Preserves `modules/anemia/evidence.json`'s existing shape (backward compatible) while adding the structured fields. | `modules/cbc_suite_v1/evidence.json` has ≥1 source per slice-rule-supporting claim; every field the `02 §4.9` table marks required is present; no `journal` value is synthesized from `title`/`publisher` (test-checked against the fixture's actual bytes) | 1.0 | general-purpose | sonnet | adaptive | P3-T1 |
| P3-T3 | `evidence-assertions.json` exact-passage projection + schema | Per FR-12 (second half), `02 §4.10`, this plan's OQ-3/OQ-7 resolutions: create `modules/cbc_suite_v1/evidence-assertions.json` (schema per `02 §4.10`'s worked example: `assertionId`, `rfRunId`, `rfSourceCardId`, `sourceId`, `rfEvidenceId`, `rfClaimId`, `passageId`, `locator`, `exactPassage`/`exactPassageSha256`, `displayPolicy`, `claimStatus`, `applicability`, `laboratory`, `reviewBy`) and `schemas/evidence-assertions.schema.json`, wired into `scripts/validate-kb.mjs` for any module directory that contains the file. Apply OQ-2's rights-restricted fallback: `exactPassage` is `null` with `exactPassageSha256` populated for any passage not positively confirmed rights-clear. | ≥1 assertion per slice-rule-supporting claim, each resolving to a real passage in the fixture; a fixture assertion missing `exactPassageSha256` when `exactPassage` is `null` fails schema validation (R-P2 analog — validator rejects the missing-field case, does not silently pass); schema wired into `scripts/validate-kb.mjs` and exercised by a passing + a seeded-failing case | 1.25 | general-purpose | sonnet | adaptive | P3-T1 |
| P3-T4 | Claim-ledger eligibility routing + `authoring-decisions.schema.json` | Per FR-13, `02 §4.11`: implement the claim-status routing table exactly — `supported` → `source_supported_fact` (only with a resolved exact passage); `mixed` → conflict-visible authoring object only, never a one-sided rule; `contradicted` → never the sole positive basis for a rule; `inference` → implementation-proposal input requiring a populated `inference_basis.from_claims`; `speculation`/`unsupported` → never emitted as rule evidence at all. Author `schemas/authoring-decisions.schema.json` (validates P3-T1's YAML, parsed to JSON) and wire it into `scripts/validate-kb.mjs`. | A `mixed`-status stub claim run through this routing produces a conflict-visible object, never a rule proposal; a `speculation`-status stub claim produces zero rule-evidence output; a `authoring-decisions.yaml` record missing `basis.reasoning` fails schema validation (R-P2 analog) | 1.25 | general-purpose | sonnet | adaptive | P3-T3 |
| P3-T5 | Candidate + rule-proposal drafting | Per `02 §4.13`, `02 §4.14`: draft `rule-proposals.json` (rich authoring metadata, one entry per slice rule matching its `authoring-decisions.yaml` record) and `candidates.json` entries for the iron-deficiency-anemia pattern per the `02 §4.14` field table (`id`/`label` say "pattern," never diagnostic certainty; `summary` preserves conflict visibility; `evidence[]` resolves in the registry; ≥1 rule must emit the candidate). Output stays in `build/kb-pack/cbc_suite_v1/0.1.0-proposal/` (gitignored staging, per P1-T7) — this task does not yet commit into `modules/cbc_suite_v1/` (that is Phase 4's job). | `rule-proposals.json` has exactly 4 entries, each joined to a P3-T1 decision record by `decisionId`; the iron-deficiency candidate's `label` contains "pattern," not a diagnostic assertion; a test confirms ≥1 proposal rule references the candidate | 1.0 | general-purpose | sonnet | adaptive | P3-T4 |
| P3-T6 | Strict runtime projection + `rule-provenance.json` + schema | Per FR-15, `02 §4.13`: emit the strict 5-field runtime projection (`id`, `category`, `when`, `evidence`, `output`, `additionalProperties: false`, matching `schemas/rule.schema.json` exactly) for each of the 4 slice rules into the staged pack's `rules.json`, and the companion `rule-provenance.json` (per `02 §4.13`'s provenance table: `ruleId`, `moduleId`, `basis.{kind, decisionId, rfClaimIds, evidenceAssertionIds}`, `missingness`, `localProfileRequirement`, `testIds`, `reviewStatus: "draft"`), joined by rule `id`. Author `schemas/rule-provenance.schema.json`, wired into `scripts/validate-kb.mjs`. The compiler must not silently discard metadata not permitted by `rule.schema.json` — every dropped field must appear in the provenance sidecar, and a test proves the join. | Each staged rule validates against `schemas/rule.schema.json` with zero errors; every field the strict schema rejects (version/effective dates, implementation-decision status, approvers, test IDs, supersession) appears in `rule-provenance.json` instead; a `rule-provenance.json` entry missing `basis.decisionId` fails schema validation (R-P2 analog) | 1.0 | general-purpose | sonnet | adaptive | P3-T5 |
| P3-T7 | `propose` verb + conflict-visibility test | Per `02 §4.5`, `02 §4.6` phases 4-9: wire P3-T1..T6 into the `propose` verb stubbed in P2-T1, assembling the full staged pack at `build/kb-pack/cbc_suite_v1/0.1.0-proposal/` (`pack-provenance.json`, `evidence.json`, `evidence-assertions.json`, `candidates.json`, `rule-proposals.json`, `rules.json`, `rule-provenance.json`). Add `tests/ef-converter-propose.test.mjs` proving `propose` run against the P1-T6 fixture yields a schema-valid pack, and that a stub `mixed`/`contradicted` claim never produces a one-sided rule (seam invariant 8). | `propose` run against the fixture succeeds and every emitted file validates against its schema; the conflict-visibility test fails if a `mixed` claim is ever the sole basis for a generated rule | 1.0 | general-purpose | sonnet | adaptive | P3-T6 |
| P3-GATE | `task-completion-validator` gate | Verify Phase 3 exit gate: `propose` yields a schema-valid pack; `mixed`/`contradicted` claims stay conflict-visible. | All exit-gate criteria pass | — | task-completion-validator | sonnet | adaptive | P3-T1..T7 |

**Phase 3 Quality Gates:**
- [ ] `propose` run against the fixture produces a schema-valid pack under `build/kb-pack/` (never committed)
- [ ] Zero rule proposal has a `mixed`/`contradicted` claim as its sole positive basis
- [ ] All 4 new/extended schemas (`evidence-assertions`, `authoring-decisions`, `rule-provenance`) reject a fixture missing a required field

---

## Phase 4: Vertical Slice + Test Corpus

**Duration**: ~4-5 engineer-days
**Dependencies**: Phase 3 complete
**Assigned Subagent(s)**: general-purpose executor (rule migration), testing specialist (test corpus);
task-completion-validator gate
**Integration Owner**: general-purpose executor — owns `modules/cbc_suite_v1/rules.json` and adjudicates
between the migrated-rule tasks (P4-T1..T4) and the generated-test tasks (P4-T5..T8).
**Seam Task** (R-P3): P4-T9 — full engine-test run proving the two specialties' outputs agree.
**Exit gate** (decisions block §3): slice rules pass engine tests; the dangerous-miss set covers the
slice-relevant hazard; no invented thresholds — every value resolves to an exact passage.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|---------------|
| P4-T1 | Migrate rule (a): young-infant/age-under-6-months scope-abstention | Per FR-16(a): commit the P3-T7 staged proposal's abstention rule into `modules/cbc_suite_v1/rules.json`, `evidence.json`, `evidence-assertions.json`, `rule-provenance.json` (module.json's `status` stays `"unsigned-stub"` — this is a migration, not a release). Verify every numeric/age-boundary literal in the rule resolves to an exact passage in `evidence-assertions.json`, not an invented threshold. | Rule committed and passes `schemas/rule.schema.json`; the age-6-months boundary resolves to a real `evidence-assertions.json` entry; `npm run validate` passes for `cbc_suite_v1` | 0.75 | general-purpose | sonnet | adaptive | P3-GATE |
| P4-T2 | Migrate rule (b): local-lab-range-precedence-over-universal-threshold | Per FR-16(b): commit the local-range-precedence rule, matching `authoring-decisions.yaml`'s `dec_cbc_local_range_precedence`-class record (P3-T1). Verify the rule fails closed (abstains) rather than applying a universal threshold when no compatible local profile exists — this is the specific behavior `02 §4.12`'s worked example authoring decision describes. | Rule committed and schema-valid; a test proves the rule abstains (does not silently apply a universal cutoff) when the local-profile fact is absent | 0.75 | general-purpose | sonnet | adaptive | P4-T1 |
| P4-T3 | Migrate rule (c): iron-deficiency-anemia candidate pattern | Per FR-16(c): commit the iron-deficiency candidate-pattern rule and its `candidates.json` entry (P3-T5), preserving "pattern," not diagnostic-certainty, wording. | Rule + candidate committed and schema-valid; candidate `label` contains "pattern"; rule emits `output.type: "candidate"` referencing the committed candidate ID | 0.75 | general-purpose | sonnet | adaptive | P4-T1 |
| P4-T4 | Migrate rule (d): marrow-red-flag safety rule | Per FR-16(d): commit the marrow-red-flag safety alert rule (`output.type: "alert"`, `severity` per `02 §4.6` phase 7's fact/type/unit/missingness resolution requirement). This is the safety-critical slice rule the Phase 4 exit gate and dangerous-miss testing (P4-T8) both key off — verify its evidence resolves to `02 §5.4`'s "multilineage cytopenia and marrow failure/infiltration" hazard specifically, not a paraphrase. | Rule committed and schema-valid; `output.severity` is `"emergency"` or `"urgent"` per the resolved evidence, never a generic default; evidence resolves to the exact `02 §5.4` hazard wording in `rule-provenance.json`'s basis | 1.0 | general-purpose | sonnet | adaptive | P4-T1 |
| P4-T5 | Test corpus — rule (a) | Per FR-17: `tests/ef-cbc_suite_v1-positive.test.mjs`, `tests/ef-cbc_suite_v1-negative.test.mjs`, `tests/ef-cbc_suite_v1-boundary.test.mjs`, `tests/ef-cbc_suite_v1-missingness.test.mjs` each gain ≥1 case for rule (a): positive (activates below 6 months), negative (does not activate at 6+ months), boundary (exactly 6 months, `>=` vs `>` correctness), missingness (age absent → question/abstention, never treated as "not young infant"). | All 4 test files contain a passing case for rule (a); the boundary case specifically exercises the `>=`/`>` operator at the 6-month edge | 0.75 | testing specialist | sonnet | adaptive | P4-T1 |
| P4-T6 | Test corpus — rule (b) | Per FR-17: positive/negative/boundary/missingness cases for the local-range-precedence rule in the same 4 flat test files — boundary case covers the local-profile-present-vs-absent edge (not a numeric edge, since this rule's "boundary" is profile availability); missingness case proves an absent local profile abstains rather than defaulting to a universal threshold. | All 4 test files contain a passing case for rule (b); missingness case specifically asserts no universal-threshold fallback occurs | 0.75 | testing specialist | sonnet | adaptive | P4-T2 |
| P4-T7 | Test corpus — rule (c) | Per FR-17: positive/negative/boundary (ferritin threshold edge)/missingness (ferritin absent) cases for the iron-deficiency candidate pattern. | All 4 test files contain a passing case for rule (c); boundary case exercises the exact ferritin cutoff resolved from `evidence-assertions.json` | 0.75 | testing specialist | sonnet | adaptive | P4-T3 |
| P4-T8 | Test corpus — rule (d) + dangerous-miss | Per FR-17, `02 §5.4`: positive/negative/boundary/missingness cases for the marrow-red-flag rule, **plus** ≥1 dangerous-miss case at `tests/ef-cbc_suite_v1-dangerous-miss.test.mjs` proving the safety alert activates and dominates ranking even when a co-occurring benign high-scoring candidate (e.g., a mild-anemia iron-deficiency pattern match) is present in the same input — the exact scenario `02 §5.4`'s "benign high-scoring candidate distracting from a higher-severity alert" hazard names. | All 4 standard test files contain a passing case for rule (d); the dangerous-miss test constructs an input matching both rule (c) and rule (d) simultaneously and asserts the alert is present and ranked/surfaced above the candidate, not suppressed by it | 1.5 | testing specialist | sonnet | extended | P4-T4, P4-T3 |
| P4-T9 | Engine integration run (seam task) | Per FR-17's overall coverage requirement and R-P3: run the full `npm test` suite against the committed `modules/cbc_suite_v1/` package end to end through `src/ruleEngine.js`/`src/engine.js`'s `assess(input, 'cbc_suite_v1', rules, candidates)` path, reconciling P4-T1..T4's committed rules against P4-T5..T8's generated tests. Confirm the module registry wiring from P1-T3 (delegated `deriveFacts`) produces correct facts for every generated test's input. | `npm test` is green including all 5 new `ef-cbc_suite_v1-*.test.mjs` files; `assess(..., 'cbc_suite_v1', ...)` produces non-crashing, correctly-ranked output for every generated test input; integration owner sign-off recorded | 1.0 | general-purpose, testing specialist | sonnet | adaptive | P4-T5, P4-T6, P4-T7, P4-T8 |
| P4-GATE | `task-completion-validator` gate | Verify Phase 4 exit gate: slice rules pass engine tests; dangerous-miss case covers the marrow-red-flag-vs-benign-candidate hazard; every numeric resolves to an exact passage. | All exit-gate criteria pass | — | task-completion-validator | sonnet | adaptive | P4-T1..T9 |

**Phase 4 Quality Gates:**
- [ ] Exactly the 4 named rules are migrated — no additional rules
- [ ] Each rule has a complete positive/negative/boundary(where numeric)/missingness test set
- [ ] The marrow-red-flag rule has ≥1 passing dangerous-miss test proving alert dominance over a co-occurring benign candidate
- [ ] `modules/cbc_suite_v1/rules.json` validates against `schemas/rule.schema.json` with zero errors
- [ ] Zero invented thresholds — every numeric literal resolves to an `evidence-assertions.json` entry

---

## Phase 5: Manifest & Traceability

**Duration**: ~3 engineer-days
**Dependencies**: Phase 4 complete
**Assigned Subagent(s)**: general-purpose executor (sonnet); task-completion-validator gate; **karen
milestone review** (decisions block §4 — "E0 functionally complete")
**Exit gate** (decisions block §3): manifest content-hash reproducible across two runs; conversion
report enumerates every exclusion with reason.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|---------------|
| P5-T1 | `release-manifest.unsigned.json` + schema | Per FR-18, `02 §4.18` minus the `signature` block: emit `build/kb-pack/cbc_suite_v1/0.1.0-proposal/release-manifest.unsigned.json` binding `rfInputs[].{runId, bundleSha256, claimLedgerSha256, verificationExitCode}`, `converter.{name, version, configSha256}`, `testCorpusHash`, `traceabilityHash`. Author `schemas/release-manifest.schema.json`. Wire it into `scripts/validate-kb.mjs` as an existence-gated check (since `build/kb-pack/` is gitignored/ephemeral per P1-T7 — the check runs only when the directory is present, per this plan's OQ-5/6/7 resolution note), and complete P2-T7's `verify` verb pack-output-validation stub against this schema. | Manifest emitted and validates against the new schema; a fixture manifest missing `rfInputs[].bundleSha256` fails validation (R-P2 analog); `verify --pack ... --rule-schema schemas/rule.schema.json` now validates the manifest too, closing P2-T7's stub | 1.25 | general-purpose | sonnet | adaptive | P4-GATE |
| P5-T2 | `conversion-report.json` | Per FR-19: enumerate every excluded or rejected claim, source, or candidate item with its specific exclusion reason (not a pass/fail summary) — every `speculation`/`unsupported` claim in the fixture that P3-T4's routing rejected must appear here with its specific reason, not be silently dropped. | Report is non-empty, structured JSON; every claim P3-T4 excluded from rule evidence appears in the report with a named reason; a test asserts the report's exclusion count matches the routing logic's actual reject count | 1.0 | general-purpose | sonnet | adaptive | P5-T1 |
| P5-T3 | `semantic-diff.json` (OQ-4) | Implements this plan's OQ-4 resolution: rule-`id`-level added/removed/changed comparison between the staged `cbc_suite_v1` proposal and `modules/anemia/rules.json` — no impact-graph traversal. Sorted, deterministic output. | `semantic-diff.json` reports exactly 4 `added` rule IDs (the slice rules), 0 `removed`, 0 `changed`, against `modules/anemia/rules.json`; output is byte-identical across two runs with unchanged inputs | 0.75 | general-purpose | sonnet | adaptive | P5-T1 |
| P5-T4 | Traceability index | Per `02 §4.16`: build the `source → passage → claim → decision → rule → test → output` index for the 4 slice rules and prove the two required bidirectional queries from `02 §4.16` work — (1) given a rendered output, show rule/decision/claims/passages/sources/review-date; (2) given a source, list claims/rules/tests/outputs potentially affected. | Both bidirectional queries succeed with zero dangling edges for all 4 slice rules; index is a committed, inspectable artifact (not a runtime-only in-memory structure) | 1.0 | general-purpose | sonnet | adaptive | P5-T1 |
| P5-T5 | Determinism double-run proof | Per FR-20, `02 §2.3` invariant 13 (High risk hotspot per decisions block §5): run `propose` twice against byte-identical inputs on the same converter version and assert SHA-256 equality across every emitted artifact (`pack-provenance.json`, `evidence.json`, `evidence-assertions.json`, `candidates.json`, `rule-proposals.json`, `rules.json`, `rule-provenance.json`, the P5-T1..T4 outputs). Add `tests/ef-converter-determinism.test.mjs` and `tests/ef-converter-manifest.test.mjs` (the latter covering P5-T1/T2's schema + exclusion-reason ACs). No timestamps embedded in hashed content; sorted serialization throughout. | Two clean `propose` runs produce byte-identical output for every emitted file (SHA-256 equality demonstrated by the test, not merely asserted in prose); both new test files pass | 1.0 | general-purpose | sonnet | extended | P5-T2, P5-T3, P5-T4 |
| P5-GATE1 | `task-completion-validator` gate | Verify Phase 5 exit gate: manifest hash reproducible; conversion report enumerates every exclusion. | All exit-gate criteria pass | — | task-completion-validator | sonnet | adaptive | P5-T1..T5 |
| P5-GATE2 | `karen` milestone review — E0 functionally complete | Independently re-verify the full E0 acceptance checklist (`02 §9.1`) against the actual repository state: determinism, zero dangling trace edges, strict schema validation, zero invented thresholds, no artifact described as clinically validated or release-ready. | `karen` sign-off recorded against `02 §9.1`'s checklist item-by-item, not just this plan's exit gate | — | karen | sonnet | adaptive | P5-GATE1 |

**Phase 5 Quality Gates:**
- [ ] Manifest content-hash is reproducible across two clean runs (SHA-256 equality demonstrated)
- [ ] Conversion report enumerates every exclusion with a specific reason
- [ ] Semantic diff reports exactly the 4 expected added rule IDs, deterministically
- [ ] Both `02 §4.16` bidirectional traceability queries succeed with zero dangling edges
- [ ] `karen` E0-complete sign-off recorded

[Return to Parent Plan](../evidence-foundry-buildout-v1.md)
