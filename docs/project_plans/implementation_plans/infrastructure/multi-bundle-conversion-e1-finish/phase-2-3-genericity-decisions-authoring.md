# Phase 2-3: Module-Generic Substrate & Decisions Authoring

[Return to Parent Plan](../multi-bundle-conversion-e1-finish.md)

**Column conventions**: same as the Phase 0-1 file. All build/authorship tasks in both phases below
are **MUST-stay-primary** (`native`, `claude-sonnet-5` or `claude-opus-4-8`) — no fallback chain by
design. Adversarial-review rows route to `gpt-5.6-terra` (flags only, never approves, never authors).

**Executable-AC discipline**: identical to the Phase 0-1 file — every AC below runs on `node:test`,
Node ≥20, no DOM/snapshot/browser-driver dependency anywhere.

---

## Phase 2: Module-Generic Drafting Substrate (**MUST-stay-primary**)

**Duration**: ~3 engineer-days
**Dependencies**: Phase 1 complete and green (the fail-closed gate must exist before this phase
removes the module-identity hard-code — the safety interlock; see parent plan)
**Assigned Subagent(s)**: native Claude only (`claude-sonnet-5`, `extended` effort for the registry
replacement, `adaptive` elsewhere); `task-completion-validator` gate
**Exit gate** (decisions block §1, PRD Goal 3/FR-F9-F10): all 3 previously-bespoke modules reach the
(now code-enforced) emission gate instead of a `UsageError`, refused by the *gate*, not by the
mechanical module-identity check; `cbc_suite_v1`'s `propose` output is SHA-256 byte-identical,
file-by-file, to its pre-change manifest — the regression anchor for this entire plan.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Provider | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|----------|--------------|
| P2-T1 | Pre-change SHA-256 manifest of `cbc_suite_v1`'s `propose` output (FR-F9 — **must run before any other task in this phase**) | Run `propose` for `cbc_suite_v1` against the current, post-Phase-1, pre-Phase-2 code, and capture a SHA-256 hash of every file `propose` emits (`pack-provenance.json`, `evidence.json`, `evidence-assertions.json`, `candidates.json`, `rule-proposals.json`, `rules.json`, `rule-provenance.json`, `release-manifest.unsigned.json`, `conversion-report.json`). Commit the manifest as a fixture (`tests/fixtures/p2-t1-cbc-propose-manifest.json`), mirroring `tests/ef-converter-determinism.test.mjs`'s existing harness shape. **This is the hard prerequisite for P2-T4 and this plan's Risk 2/R-2 mitigation** — without it, the byte-identity exit gate for this entire feature is unverifiable, per this plan's own binding instructions. | `tests/fixtures/p2-t1-cbc-propose-manifest.json` is committed, containing exactly 9 file-hash entries; a smoke test confirms the manifest's hashes match a fresh `propose` run's live output taken BEFORE P2-T3 lands (i.e., captured on the Phase-1-complete commit, not after) | 0.5 | native | claude-sonnet-5 | adaptive | claude | Phase 1 complete |
| P2-T2 | Front-loaded spike-let: kidney/growth `evidence-assertions.json` completeness for runtime cross-resolution (decisions block estimation note, Risk 4/R-7) | SPIKE-009 verified `anemia`'s `evidence-assertions.json` in depth (35 real `evas_anemia_*` records) but only spot-checked kidney (73 assertions) and growth (79 assertions) for shape parity. Before committing to the full P3 authoring effort, write a short `node:test` check parsing all 3 modules' `evidence-assertions.json` and asserting each assertion record has the same required-field shape (`assertionId`, `rfClaimId`, `rfSourceCardId`, `exactPassageSha256`) as anemia's, and that at least one real, resolvable `evas_*` id exists per module suitable for P3's authoring to bind against. | Test passes for all 3 modules; if any module's assertions are found shape-deficient, this task's own output is a named list of the specific deficiencies (not a silent pass) — feeding directly into P3's authoring task estimates rather than being discovered mid-P3 | 0.5 | native | claude-sonnet-5 | adaptive | claude | Phase 1 complete |
| P2-T3 | Replace hard-coded `MODULE_ID` with a per-module drafting-content registry (FR-F10, OQ-2) | Convert `rule-candidate-drafts.mjs`'s single-scalar `MODULE_ID = 'cbc_suite_v1'` + `RULE_PROPOSALS` into a `moduleId`-keyed registry (`RULE_PROPOSAL_REGISTRY = { cbc_suite_v1: RULE_PROPOSALS }`, defaulting to `[]` for any unregistered module id — including all 3 new modules, since none has hand-authored rule-body content in this pass). `propose.mjs` selects `RULE_PROPOSAL_REGISTRY[pinned.moduleId] ?? []` instead of throwing `UsageError` on a moduleId mismatch (`propose.mjs:568-575`'s throw site is removed entirely). The array so selected is then filtered through Phase 1's `approved_for_rule_draft` allowlist gate (already landed) before `writeStagedRulesAndProvenance()` is ever called — genericity and the fail-closed gate compose, they do not duplicate each other's job. Per OQ-2: the registry itself stays vestigial/empty for 3 of 4 modules; the actual gating signal remains 100% decisions-file-driven, honoring the "derive from decisions file" prior functionally even though the rule-BODY content (where it exists at all) is still, unavoidably, hand-authored code data — never inferred from `basis.reasoning` prose (FR-14, unchanged). | `propose` no longer throws `UsageError` for module identity for any of the 4 registered modules; running `propose` for `anemia`/`kidney_suite_v1`/`growth_suite_v1` (once Phase 3's decisions files exist) reaches Phase 1's emission gate and is refused with a NAMED governance reason (`RuleEmissionRefusedError`), never a `UsageError`; `cbc_suite_v1`'s registry entry is unchanged (`RULE_PROPOSAL_REGISTRY.cbc_suite_v1 === RULE_PROPOSALS`, same array reference) | 2.5 | native | claude-sonnet-5 | extended | claude | P2-T1 |
| P2-T4 | `cbc_suite_v1` byte-identity regression test (FR-F10 exit gate, Risk 2) | Author `tests/ef-cbc-byte-identity-regression.test.mjs`: run `propose` for `cbc_suite_v1` post-P2-T3 and SHA-256-compare every one of the 9 emitted files against P2-T1's committed manifest. Any single-byte drift is a hard failure — per this plan's NFR, a `cbc_suite_v1` output drift is a clinical content change, not a build break. | Test passes; all 9 files match the P2-T1 manifest byte-for-byte; test is written so it FAILS LOUDLY (not silently skips) if the manifest fixture itself is missing | 0.75 | native | claude-sonnet-5 | adaptive | claude | P2-T1, P2-T3 |
| P2-T5 | Fix shared-mutable-state test hazard (FR-F17 — **re-sequenced here from the PRD's rough P5 mapping**; see parent plan's FR coverage matrix note) | Rewrite `tests/ef-converter-rule-candidate-drafting.test.mjs` and `tests/ef-converter-rule-provenance-projection.test.mjs` to use `mkdtemp(path.join(os.tmpdir(), ...))` scratch directories instead of the real, shared `build/kb-pack/cbc_suite_v1/0.1.0-proposal` directory both currently write into (one of them even `rm()`s it first) — the exact pattern `tests/ef-multi-bundle-determinism.test.mjs` and this same file's OTHER test (`writeStagedRulesAndProvenance({ outDir: tempDir })`) already prove works for this precise converter surface. Done here (not P5) because P2-T6 already has both files open for the registry-shape update below — fixing both in the same pass avoids re-opening already-closed files. | Both test files use `mkdtemp` scratch dirs exclusively; zero write to the shared `build/kb-pack/cbc_suite_v1/0.1.0-proposal` path from either file (grep-confirmed); tests still pass, unchanged in assertions, only in directory isolation | 0.75 | native | claude-sonnet-5 | adaptive | claude | P2-T3 |
| P2-T6 | Update existing propose/batch test suites for the new registry shape | `tests/ef-converter-rule-candidate-drafting.test.mjs`, `tests/ef-converter-rule-provenance-projection.test.mjs`, `tests/ef-converter-propose.test.mjs`, `tests/ef-converter-batch.test.mjs`, and `tests/ef-batch-runner.test.mjs` currently assert a `UsageError` is thrown for any non-`cbc_suite_v1` module — that assertion is now WRONG (per P2-T3, the identity throw is removed) and must be updated to assert the module instead reaches Phase 1's governance-refusal gate. Also re-verify Phase 1's `tests/ef-rule-emission-invariant.test.mjs` (P1-T6) and negative-control test (P1-T5) both stay green post-P2-T3 (FR-F24's "stays green through P2" requirement). | All 5 listed test files pass with updated assertions (governance refusal, not usage error, for the 3 non-cbc modules); `tests/ef-rule-emission-invariant.test.mjs` and `tests/ef-converter-emission-gate.test.mjs` both re-verified green in this same task, cited explicitly at P2-GATE | 1.5 | native | claude-sonnet-5 | adaptive | claude | P2-T3, P2-T5 |
| P2-GATE | `task-completion-validator` gate | Verify: `cbc_suite_v1` byte-identity holds (P2-T4); all 3 non-cbc modules reach the emission gate, not `UsageError`; FR-F24's invariant test (P1-T6) is re-confirmed green; the shared-mutable-state hazard is closed (P2-T5); the adversarial review from Phase 1 has no unresolved finding that this phase's changes reopen. | All exit-gate criteria pass; recorded in phase progress note | — | task-completion-validator | claude-sonnet-5 | adaptive | claude | P2-T1..T6 |

**Phase 2 Quality Gates:**
- [ ] `cbc_suite_v1`'s `propose` output is SHA-256 byte-identical, file-by-file, to the P2-T1 manifest
- [ ] `anemia`/`kidney_suite_v1`/`growth_suite_v1` reach the (Phase 1) emission gate instead of the module-identity `UsageError`
- [ ] FR-F24's repo-level invariant test (P1-T6) re-verified green post-genericity refactor
- [ ] Shared-mutable-state test hazard closed (FR-F17, this phase, not P5)
- [ ] Zero task in this phase populated `approvedBy[]`/`clinicalApprovers[]` on any module

---

## Phase 3: Author 3× Non-Approving Decisions Files (**MUST-stay-primary, zero delegation of authorship**)

**Duration**: ~3-4 engineer-days (SPIKE-009's own 5-8 pt estimate for authoring alone, ~85% clinical/
evidentiary judgment, plus this plan's added scope-lock/cross-resolution/adversarial/verdict tasks)
**Dependencies**: Phase 2 complete and green (decisions-file content must actually drive behavior
before authoring these files is meaningfully testable — authoring them against a converter that
ignores them would produce unfalsifiable artifacts, the exact failure mode this feature exists to
correct)
**Assigned Subagent(s)**: native Claude (`claude-sonnet-5`, draft) → `claude-opus-4-8` (mandatory
verdict pass, `xhigh` effort) — **zero delegation of any kind for authorship**; `gpt-5.6-terra`
adversarial review (flags only); `task-completion-validator` + `karen` (mid-feature milestone review)
**Exit gate** (decisions block §1, PRD Goal — FR-F11/F12/F13): files are schema-valid and
ID-resolvable; negative-control asserting no file contains `approved_for_rule_draft`; zero numeric
threshold appears that is not traceable to a cited claim.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Provider | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|----------|--------------|
| P3-T1 | Lock `propose`'s output scope for the 3 non-cbc modules (FR-F11) | Author a positive-allowlist test (`tests/ef-propose-output-scope-lock.test.mjs`) asserting that a `propose` run for `anemia`/`kidney_suite_v1`/`growth_suite_v1` writes ONLY: `pack-provenance.json`, `evidence.json`, `evidence-assertions.json`, `candidates.json`, `unresolved.json`, `conversion-report.json`, `semantic-diff.json` (Phase 4), and an **inert** `rule-proposals.json` — and NEVER `rules.json`/`rule-provenance.json`. This is belt-and-suspenders on top of Phase 1/2's gate (which already guarantees this outcome mechanically) — an explicit, separate, positive assertion of the exact file inventory, not merely the absence of two specific files. | Test passes for all 3 modules; test enumerates the exact expected file set (a bounded list, per R-P1) and fails if ANY unexpected file appears in the output tree, not only if `rules.json` appears | 0.5 | native | claude-sonnet-5 | adaptive | claude | Phase 2 complete |
| P3-T2 | Author `modules/anemia/authoring-decisions.yaml` (new file; FR-F5, FR-F12) | Hand-author a schema-valid decisions file for `anemia`: `moduleId: anemia`, `rfProvenance` block from `rf-ev-001`'s real `run_id`/`bundle_id`/fixture path, ≥2 decision records binding real `clm_*` ids from `tests/fixtures/rf-ev-001/claims/claim_ledger.yaml` and real `evas_anemia_*` ids from `modules/anemia/evidence-assertions.json` (per P2-T2's confirmed shape). **Every decision's `status` is `drafted_pending_human_approval`** (never `approved_for_rule_draft`); every `review.*` role is `pending`. `basis.reasoning` paraphrases ONLY the cited claim's own text — no invented threshold, no clinical judgment beyond what the cited claim supports. | File is new (git-confirmed, not an edit to any pre-existing file); schema-valid; ≥2 decision records; every `status` is `drafted_pending_human_approval`; every `review.*` is `pending`; a reviewer spot-check (P3-T7) confirms no numeric value in any `reasoning` field is absent from its cited claim's own text | 2.0 | native | claude-sonnet-5 | extended | claude | Phase 2 complete, P2-T2 |
| P3-T3 | Author `modules/kidney_suite_v1/authoring-decisions.yaml` (new file; FR-F5, FR-F12) | Same pattern as P3-T2, binding to `tests/fixtures/rf-kid-001/claims/claim_ledger.yaml` and `modules/kidney_suite_v1/evidence-assertions.json` (73 real assertions, confirmed by P2-T2). | Same AC shape as P3-T2, applied to `kidney_suite_v1` | 2.0 | native | claude-sonnet-5 | extended | claude | Phase 2 complete, P2-T2 |
| P3-T4 | Author `modules/growth_suite_v1/authoring-decisions.yaml` (new file; FR-F5, FR-F12) | Same pattern as P3-T2, binding to `tests/fixtures/rf-gro-002/claims/claim_ledger.yaml` and `modules/growth_suite_v1/evidence-assertions.json` (79 real assertions, confirmed by P2-T2). | Same AC shape as P3-T2, applied to `growth_suite_v1` | 2.0 | native | claude-sonnet-5 | extended | claude | Phase 2 complete, P2-T2 |
| P3-T5 | Schema-validate + cross-resolve all 3 new files (FR-F13) | Reuse Phase 1's runtime resolver (P1-T4's `clm_*`/`evas_*` cross-resolution) as the validation mechanism for all 3 new files — not a bespoke one-off check, per FR-F13's explicit instruction. Run each file's `decisions[]` through the same resolver `propose` itself would invoke. | All 3 files pass schema validation; every cited `clm_*`/`evas_*` id in all 3 files resolves against that module's real fixtures (zero `UnresolvedClaimReferenceError`); the SAME resolver code path is used here as in Phase 1 (not a duplicate implementation — grep-confirmed shared import) | 0.75 | native | claude-sonnet-5 | adaptive | claude | P3-T2, P3-T3, P3-T4 |
| P3-T6 | Adversarial review hunting for invented thresholds (Risk 5, decisions block §6) | `gpt-5.6-terra` read-only review of all 3 new decisions files, specifically hunting for: any numeric value in `reasoning`/`basis` not traceable to the cited claim's own text; any `clinical_effect.intended_output`/`conflicts.representation` label implying more clinical judgment than the cited claims support; any accidental `approved_for_rule_draft` or non-`pending` `review.*` value. Flags only — findings are adjudicated by the Opus verdict pass (P3-T7), never auto-applied. | Review completes and produces a written finding list (even if empty); every finding is explicitly adjudicated before P3-GATE closes | 0.5 | gpt-5.6-terra | gpt-5.6-terra | medium | codex | P3-T2, P3-T3, P3-T4 |
| P3-T7 | Opus verdict pass (FR-F5, mandatory, non-delegable) | `claude-opus-4-8` sign-off confirming, across all 3 new files: zero invented thresholds; zero `approved_for_rule_draft` anywhere; all `review.*` fields `pending`; zero `approvedBy[]`/`clinicalApprovers[]` population anywhere in this phase's diff; P3-T6's adversarial findings are all resolved. This pass closes the phase — it is not optional and is not delegated to sonnet. | Written verdict recorded (approve or send-back-for-revision); phase does not close without an explicit Opus approval | 0.75 | native | claude-opus-4-8 | xhigh | claude | P3-T5, P3-T6 |
| P3-GATE | `task-completion-validator` + `karen` (mid-feature milestone) | Verify: all 3 files exist, are new (not edits), schema-valid, cross-resolved; FR-F11's scope lock (P3-T1) holds; Opus verdict (P3-T7) recorded approval; `karen` independently re-checks (spot, not exhaustive) that no numeric threshold in any file is untraceable to a cited claim. | All exit-gate criteria pass; `karen` sign-off recorded in phase progress note | — | task-completion-validator, karen | claude-sonnet-5 | adaptive | claude | P3-T1..T7 |

**Phase 3 Quality Gates:**
- [ ] `modules/{anemia,kidney_suite_v1,growth_suite_v1}/authoring-decisions.yaml` are all NEW files (git-confirmed), schema-valid, cross-resolved
- [ ] Every decision in all 3 files carries `status: drafted_pending_human_approval`; zero `approved_for_rule_draft` anywhere (negative-control test-asserted)
- [ ] All four `review.*` roles are `pending` in every decision, in every file
- [ ] Zero numeric threshold in any file is untraceable to a cited `clm_*` claim's own text (adversarially reviewed AND Opus-verdicted, not merely test-checked)
- [ ] Zero task in this phase populated `approvedBy[]`/`clinicalApprovers[]`
- [ ] FR-F11's output-scope lock (P3-T1) passes for all 3 modules

[Return to Parent Plan](../multi-bundle-conversion-e1-finish.md)
