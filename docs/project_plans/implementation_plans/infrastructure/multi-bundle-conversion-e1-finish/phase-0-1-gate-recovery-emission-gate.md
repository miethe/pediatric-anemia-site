# Phase 0-1: Gate Recovery & Fail-Closed Emission Gate

[Return to Parent Plan](../multi-bundle-conversion-e1-finish.md)

**Column conventions** (apply to every task table below): `Estimate` is story points, never Effort.
`Model` values used in this file: `claude-haiku-4-5` (ICA free-tier), `gpt-5.6-luna`, `claude-sonnet-5`
(native, MUST-stay-primary), `gpt-5.6-terra` (adversarial review, flags only). `Effort` values (claude):
`adaptive` | `extended`; (codex): `none|low|medium|high|xhigh`. See
`.claude/skills/planning/references/multi-model-guidance.md`. Gate rows
(`task-completion-validator`, `karen`) carry `Estimate: —` — reviewer checkpoints, not pointed build
work.

**Executable-AC discipline (binding for this entire plan)**: every acceptance criterion below runs on
`node:test` only, Node ≥20, zero test dependencies beyond what `package.json` already declares. No
DOM test, no snapshot library, no browser driver, no headless-browser assertion appears anywhere in
this file. A prior plan in this program was rejected by a `karen` gate for promising verification this
repo cannot build — every AC here was checked against that failure mode before being written.

---

## Phase 0: Gate Recovery — Green `npm run check` Honestly

**Duration**: ~1-2 engineer-days
**Dependencies**: None — first phase in this plan
**Assigned Subagent(s)**: `ica-executor` (rights-field plumbing, `claude-haiku-4-5`, ICA free-tier,
requires `bypassPermissions` per project memory — `acceptEdits` alone leaves gates unverified);
`codex-executor` (fixture regen, regex fix, build-order fix, `gpt-5.6-luna`); `task-completion-validator`
gate
**Exit gate** (decisions block §1): `npm run check` exits 0 from a clean tree; zero field minted by
this phase moved off the schema's own `overall_status: "UNKNOWN"` / `judgment_basis: "unassessed"`
honest-triage vocabulary.

**File-disjointness proof for this phase's two parallel batches** (per project memory — wave-plan
batches have collided in this repo before, CRW batch_2 — verified explicitly, not assumed):

| Batch | Tasks | Files touched |
|---|---|---|
| (a) Evidence projections | P0-T1, P0-T2, P0-T3 | `scripts/evidence/lib/cbc-002-projection.mjs`, `modules/kidney_suite_v1/evidence.json`, `modules/growth_suite_v1/evidence.json` |
| (b) Rights ledger/records + fixtures + regex | P0-T4, P0-T5, P0-T6, P0-T7 | `rights/rights-records.json`, `rights/rights-ledger.json`, `tests/fixtures/p4-t1-pre-merge-snapshot.json.txt`, `docs/architecture.md` |

Set (a) ∩ Set (b) = ∅ — confirmed by direct enumeration above. Batches (a) and (b) run concurrently;
P0-T8/P0-T9 run after both merge (sequential tail — P0-T9's negative-control test reads output from
both batches).

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Provider | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|----------|--------------|
| P0-T1 | Backfill `cbc_suite_v1`'s 12 RF-CBC-002 sources' rights fields (FR-F1) | Per SPIKE-009 Leg B's exact remediation step 1: extend `scripts/evidence/lib/cbc-002-projection.mjs`'s `buildNewSources()` (lines ~180-234) to emit the EPR0-T4 triage-`unknown` block on each of the 12 `SOURCE_DEFS` entries — `license: "unknown"`, `access_basis: "unknown"`, `terms: "unknown"`, `terms_snapshot: "unknown"` on each source; `evidence_item_type`, `judgment_basis: "unassessed"`, `judgment_basis_attestation: null`, `rights_component_class`, `structured_locator`, typed `not_captured` on each passage — mirroring the shape already used correctly on these sources' own `implementation-proposal` sentinel passages. Asserts nothing beyond the schema's own `unknown`/`unassessed` enum members. | `node scripts/validate-kb.mjs` reports zero missing-required-property errors for `cbc_suite_v1`'s 12 RF-CBC-002 sources; `tests/ef-cbc-002-backfill.test.mjs#buildNewSources` (previously failing, test #348 per SPIKE-009) passes; every new field's value is one of the schema's own `unknown`/`unassessed`/`null` closed-vocabulary members — never a specific license/basis/judgment value | 0.75 | ica-executor | claude-haiku-4-5 | low | ICA free | None |
| P0-T2 | Backfill `kidney_suite_v1`'s 12 sources' rights fields (FR-F1) | Per SPIKE-009 Leg B: no dedicated builder script exists for this module — hand-author the equivalent rights block directly into each of the 12 sources in `modules/kidney_suite_v1/evidence.json`, same field set and same honest-`unknown` values as P0-T1. | `node scripts/validate-kb.mjs` reports zero missing-required-property errors for `kidney_suite_v1`'s 12 sources; every new field is an `unknown`/`unassessed`/`null` closed-vocabulary member | 0.5 | ica-executor | claude-haiku-4-5 | low | ICA free | None |
| P0-T3 | Backfill `growth_suite_v1`'s 11 sources' rights fields (FR-F1) | Per SPIKE-009 Leg B: same treatment as P0-T2, applied to the 11 sources in `modules/growth_suite_v1/evidence.json`. | `node scripts/validate-kb.mjs` reports zero missing-required-property errors for `growth_suite_v1`'s 11 sources; every new field is an `unknown`/`unassessed`/`null` closed-vocabulary member | 0.5 | ica-executor | claude-haiku-4-5 | low | ICA free | None |
| P0-T4 | Mint 35 triage-only `rights/rights-records.json` entries (FR-F2) | Per SPIKE-009 Leg B step 2: for each of the 35 source ids across the 3 modules above, mint a new record (`RR-<sourceId>`) mirroring `RR-AAP2026_IDA` verbatim in shape: `access.basis: "unknown"`, `access.automated_retrieval_allowed: "unknown"`, `access.model_training_allowed: "not_assessed"`, `copyright.status: "unknown"`, `overall_status: "UNKNOWN"`, `review.assessed_by_agent: "multi-bundle-conversion-e1-finish-p0"` (a marker naming THIS feature, distinct from the prior pass's own markers), `review.human_reviewer: null`, `review.counsel_reviewer: null`, `review.review_status: "agent_triage_only"`. | `rights/rights-records.json` gains exactly 35 new records; every one has `overall_status: "UNKNOWN"` and `review.review_status: "agent_triage_only"`; `review.assessed_by_agent` is a string distinct from any pre-existing marker in the file (uniqueness asserted by test) | 1.0 | ica-executor | claude-haiku-4-5 | low | ICA free | None |
| P0-T5 | Mint 35 matching `rights/rights-ledger.json` join entries (FR-F2) | Per SPIKE-009 Leg B step 3: add a `kb_json_file_path`-or-`evidence_source_id`-keyed join entry (mirroring the existing 14 `cbc_suite_v1`-backfill entries' shape) for each of the 35 new ids, linking to its P0-T4 `rights_record_id`. | `node scripts/validate-kb.mjs` reports zero "no rights/rights-ledger.json entry resolves this evidence_source_id" errors for any of the 35 ids; every new entry's `rights_record_id` resolves to a real P0-T4 record | 0.5 | ica-executor | claude-haiku-4-5 | low | ICA free | P0-T4 |
| P0-T6 | Regenerate stale `tests/fixtures/p4-t1-pre-merge-snapshot.json.txt` (FR-F3) | Per SPIKE-009 Leg B step 4: the committed fixture's recorded SHA-256 for `modules/anemia/evidence.json` no longer matches the real, legitimate, committed file (verified: `git status`/`git diff` on the real file are both empty — the fixture, not the file, is stale). Regenerate via `scripts/lib/p4-t1-snapshot.mjs`'s `computeSnapshot()` (or `scripts/capture-p4-t1-snapshot.mjs`) against current HEAD. | `tests/ef-anemia-backfill-integrity.test.mjs` and its 3 sibling P4-T1-baseline tests pass; the regenerated fixture's hash for `modules/anemia/evidence.json` matches `shasum -a 256 modules/anemia/evidence.json`'s live output exactly | 0.25 | codex-executor | gpt-5.6-luna | low | codex | None |
| P0-T7 | Fix `notice-architecture-no-clearance.test.mjs` false positive (FR-F3) | The test's negation-marker regex does not recognize "rather than X" as negating X, so `docs/architecture.md`'s line 75 ("...flagged for legal review rather than cleared as clinical evidence") false-positives on the bare word "cleared". Reword the sentence to use a recognized negation marker directly adjacent to "cleared" (e.g., "...flagged for legal review and never treated as cleared clinical evidence") rather than widening the test's marker list — a doc-wording fix is lower-risk than a regex change to a rights-honesty gate. | `node --test tests/notice-architecture-no-clearance.test.mjs` passes; the reworded sentence in `docs/architecture.md` preserves its original meaning (verified by a reviewer reading the diff, not only by the test passing) | 0.25 | codex-executor | gpt-5.6-luna | low | codex | None |
| P0-T8 | Reorder `npm run check` so `build` runs before `test`; sync `CLAUDE.md` same-commit (FR-F3, OQ-6) | Per SPIKE-009 Leg B's residual failure: `tests/module-registry.test.mjs`'s `dist/`-dependent assertions (P6-010(a), P6-008(c)) fail from a genuinely clean/fresh clone because `package.json`'s `scripts.check` currently runs `npm test` (which needs `dist/`) BEFORE `npm run build` (which produces it) — a real ordering defect, not merely a local-worktree artifact. Reorder `scripts.check` to `npm run build && npm test && npm run validate && npm run coverage:rules && npm run verify:d4 && npm run check:imports && npm run smoke:browser && npm run smoke`. Update `CLAUDE.md`'s copied check-gate string in the **same commit**, per `tests/claudemd-check-gate.test.mjs`'s doc-truth requirement. | `npm run check` exits 0 from a genuinely clean, never-built clone (verified: `rm -rf dist/ node_modules/.cache 2>/dev/null; npm run check` — dist/ is gitignored so this reproduces a fresh-clone state); `tests/claudemd-check-gate.test.mjs` passes, proving `CLAUDE.md`'s documented composition is byte-identical to `package.json`'s `scripts.check` | 0.5 | codex-executor | gpt-5.6-luna | low | codex | P0-T6, P0-T7 |
| P0-T9 | Negative-control test: honest-`unknown` vocabulary, 35/35 (FR-F4, Risk 3) | Author a `node:test` case asserting every one of the 35 records/fields this phase touches (P0-T1..T5) carries only the schema's own `unknown`/`unassessed`/`null` closed-vocabulary values — zero field asserts a specific license status, access basis, or judgment-basis determination. Assert `review.assessed_by_agent` uniquely names this feature on all 35 `rights-records.json` entries. | Test passes; explicitly fails if any of the 35 touched fields/records is found with a non-`unknown`/`unassessed`/`null` value in its rights-relevant fields; test asserts it examined exactly 35 records (not fewer, not more) | 0.5 | native | claude-sonnet-5 | adaptive | claude | P0-T1..T7 |
| P0-GATE | `task-completion-validator` gate | Verify Phase 0 exit gate: `npm run check` exits 0 from a clean tree (per P0-T8's reordering); zero field moved off the honest-`unknown`/`unassessed` vocabulary (P0-T9); all 4 SPIKE-009 Leg B root causes closed. | All exit-gate criteria pass; recorded in phase progress note | — | task-completion-validator | claude-sonnet-5 | adaptive | claude | P0-T1..T9 |

**Phase 0 Quality Gates:**
- [ ] `npm run check` exits 0 from a clean tree (reordered composition: `build` before `test`)
- [ ] 35/35 new rights records carry `overall_status: "UNKNOWN"` and `review.review_status: "agent_triage_only"` — zero exceptions, proven by P0-T9's negative-control test
- [ ] `tests/ef-cbc-002-backfill.test.mjs`, `tests/ef-anemia-backfill-integrity.test.mjs` (+ 3 siblings), `tests/notice-architecture-no-clearance.test.mjs` all pass
- [ ] `tests/claudemd-check-gate.test.mjs` passes — `CLAUDE.md` and `package.json` agree byte-for-byte on the check-gate composition
- [ ] Zero converter code (`tools/rf-bundle-to-kb-pack/**`) touched in this phase — data/fixture-only, per the decisions block's phase-boundary rationale

---

## Phase 1: Fail-Closed Emission Gate Becomes Code (**MUST-stay-primary**)

**Duration**: ~2-3 engineer-days
**Dependencies**: Phase 0 complete (`npm run check` green)
**Assigned Subagent(s)**: native Claude only (`claude-sonnet-5`, `extended` effort, **no fallback
chain by design** — if primary is unavailable this phase blocks, it does not downgrade to ICA or
codex); `gpt-5.6-terra` for the mandatory adversarial read-only diff review (flags only, never
approves); `task-completion-validator` + `karen` (safety-critical milestone review)
**Exit gate** (decisions block §1, PRD Goal 2/FR-F6-F9/FR-F24): a decisions file with no
`approved_for_rule_draft` decision cannot produce `rules.json`/`rule-provenance.json`, proven by
file-absence assertion and a non-zero, named refusal reason in `conversion-report.json` — not by
inspecting prose. This must land, tested, and green **before Phase 2 begins** — see the parent
plan's `wave_plan` `depends_on` edge; this is a hard, non-negotiable ordering, not a scheduling
convenience. Removing the accidental protection (the hard-coded `MODULE_ID` check, Phase 2) before
installing the intentional one (this phase's live `status` branch) would arm AI-draftable rule
emission across `anemia`/`kidney_suite_v1`/`growth_suite_v1` with nothing but an inert documentation
field standing in the way.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Provider | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|----------|--------------|
| P1-T1 | Add `drafted_pending_human_approval` to the `status` enum (FR-F6, R-2/OQ-1) | Add the new, visibly-non-approving enum value to `schemas/authoring-decisions.schema.json`'s `decision.status` (`["approved_for_rule_draft", "rejected", "withdrawn", "drafted_pending_human_approval"]`). Confirm no existing consumer of the schema breaks — `cbc_suite_v1/authoring-decisions.yaml`'s 4 existing `approved_for_rule_draft` decisions must still validate unchanged. | Schema validates a decision record with `status: drafted_pending_human_approval`; `tests/authoring-decisions-schema.test.mjs` (extended) asserts all 4 enum members validate and a 5th, invented value does NOT; `cbc_suite_v1/authoring-decisions.yaml` still validates byte-unchanged against the new schema | 0.5 | native | claude-sonnet-5 | extended | claude | Phase 0 complete |
| P1-T2 | Live allowlist `status` branch in `propose.mjs` (FR-F6, R-2 — **the single most load-bearing implementation detail in this plan**) | Make `propose.mjs` read `pinned.decisions.parsed.decisions[]` (currently never referenced — grep-confirmed zero occurrences by SPIKE-009) and, for each decision a drafted rule/candidate would cite, branch on `status === 'approved_for_rule_draft'` as the ONLY permitting condition. **This MUST be coded as a positive membership check, never as an enumerated denylist** (`if (status === 'rejected' || status === 'withdrawn') throw` is explicitly the WRONG shape and must not appear anywhere in the diff) — an allowlist fails closed when a future enum value is added; a denylist fails open. Add `RuleEmissionRefusedError extends GovernanceError` (`tools/rf-bundle-to-kb-pack/lib/errors.mjs`, exit 3 GOVERNANCE) naming the specific refusal reason, recorded in `conversion-report.json`. | A decisions file with every decision at `drafted_pending_human_approval`, `rejected`, or `withdrawn` refuses emission identically via the SAME code path (proven by a test that seeds a 5th, never-before-seen status string and asserts it ALSO refuses — the allowlist's fail-closed-on-unknown-value property, which no denylist enumeration could guarantee); `conversion-report.json` names the refusal reason and the specific decision id(s) that failed to reach `approved_for_rule_draft` | 1.5 | native | claude-sonnet-5 | extended | claude | P1-T1 |
| P1-T3 | Conditional `writeStagedRulesAndProvenance()` — the seam task proving `cbc_suite_v1` is unaffected (FR-F6, R-P3 seam task) | Make `scripts/evidence/govern-staged-rules.mjs`'s `writeStagedRulesAndProvenance()` (currently unconditional) refuse to write `rules.json`/`rule-provenance.json` unless at least one resolved, cross-validated decision for the claim(s) in question carries `status: approved_for_rule_draft`. This is the **seam task** (R-P3): it must prove `cbc_suite_v1`'s existing 4 approved decisions still permit its existing emission path unchanged, since this repo's ONE already-committed, already-verified rule content depends on this exact function continuing to fire for it. | `cbc_suite_v1`'s `propose` run (against its own, unchanged `authoring-decisions.yaml`, 4 `approved_for_rule_draft` decisions) still emits `rules.json`/`rule-provenance.json`, byte-identical to its pre-P1 output (spot-checked here; the full SHA-256 manifest proof is P2-T1/P2-T4's job, one phase later); a decisions file with zero approved decisions never calls `writeStagedRulesAndProvenance()` at all (test-asserted via a spy/mock or via file-absence, not merely "the function returned early") | 1.0 | native | claude-sonnet-5 | extended | claude | P1-T2 |
| P1-T4 | Runtime `clm_*`/`evas_*` cross-resolution (FR-F7, OQ-3 — resolved to P1, a fabrication guard shipping with the other guards) | Add a runtime resolver invoked by `propose` that cross-checks every decision's `rf_claim_ids[]` against the bundle's own `claims/claim_ledger.yaml` and every `exact_assertion_ids[]` entry against the module's `evidence-assertions.json`. An ID that does not resolve throws `UnresolvedClaimReferenceError extends SchemaError` (new class, `tools/rf-bundle-to-kb-pack/lib/errors.mjs`, exit 2 SCHEMA) BEFORE any output is written — this is a referential-integrity extension of what the JSON Schema itself explicitly states it cannot check ("this schema cannot verify that cross-file resolution itself"). | A decisions file citing an invented `clm_99999` (absent from the real `claim_ledger.yaml`) throws `UnresolvedClaimReferenceError` naming the specific invented id, before `mkdir(outDir)` is ever called (zero partial output written); the same test repeated for an invented `evas_*` id; `cbc_suite_v1`'s real, existing 4 decisions all resolve cleanly (regression check) | 1.5 | native | claude-sonnet-5 | extended | claude | P1-T1 |
| P1-T5 | Negative-control test: gate + cross-resolution together (FR-F8) | Author `tests/ef-converter-emission-gate.test.mjs`: a decisions file where every decision is non-approving (or cites an unresolvable id) MUST NOT result in `rules.json`/`rule-provenance.json` appearing anywhere in `propose`'s output tree, and MUST result in a named, non-zero refusal captured in `conversion-report.json`. | Test passes; asserts `rules.json`/`rule-provenance.json` are absent from the output directory by `fs.access` throwing `ENOENT` (not merely "empty array") — this is FR-F8's literal wording and this plan's PRD's own success metric | 1.0 | native | claude-sonnet-5 | adaptive | claude | P1-T2, P1-T3, P1-T4, P1-T8 |
| P1-T6 | Repo-level invariant test (FR-F24) | Author `tests/ef-rule-emission-invariant.test.mjs` asserting: (a) `modules/{kidney_suite_v1,growth_suite_v1}/rules.json` stay `[]` (committed, pre-existing, unaffected by this pass); (b) `modules/anemia/rules.json`'s 91 hand-authored, pre-existing rules stay byte-identical to a pinned baseline hash (this pass never touches them — they predate the converter entirely and are NOT "traceable to converter output," distinguishing them from what this invariant actually forbids); (c) none of `anemia`/`kidney_suite_v1`/`growth_suite_v1` has a `rule-provenance.json` file (only `cbc_suite_v1` does). This test must stay green through every subsequent phase (P2/P3/P4) — re-verified at each phase's GATE row. | Test passes now; test is re-run and re-verified green at P2-GATE, P3-GATE, and P4-GATE (cited explicitly in each of those rows, not merely assumed) | 0.75 | native | claude-sonnet-5 | adaptive | claude | P1-T2, P1-T3, P1-T4, P1-T8 |
| P1-T8 | Make a governance refusal a caught, non-fatal signal in `propose.mjs`'s emission ordering (FR-F6, FR-F8, FR-F11 — **planning-gate BLOCKING-finding fix**) | Today, P1-T3 makes `writeStagedRulesAndProvenance()` conditional, but `propose.mjs`'s subsequent, unconditional `readFile(rulesPath)`/`readFile(ruleProvenancePath)` calls (~line 634, feeding the release-manifest traceability hash and `semantic-diff.json`'s `headRules` via `JSON.parse(rulesRaw)`) then throw `ENOENT` on a refused module — an uncaught exception escaping `run()`, so `conversion-report.json` (written AFTER this readback, ~line 660) and `semantic-diff.json` are NEVER written, making FR-F8/FR-F11 unreachable; worse, `BATCH_PAIRS[0]` is `rf-ev-001` → `modules/anemia` (a refused module) and `batch` halts on first failure, so 4-of-4 never reaches `cbc_suite_v1`. Restructure `propose.mjs` so the refusal (already computed by P1-T2/T3's gate) is captured as a value BEFORE the read is attempted, not discovered via a caught filesystem exception: on refusal, `rulesRaw`/`ruleProvenanceRaw` are each the deterministic empty string `''` (never a file read) — `computeTraceabilityHash({ decisionsRaw, evidenceAssertionsRaw, ruleProvenanceRaw: '', rulesRaw: '' })` is still the SAME pure function, over the two real inputs plus two empty-string placeholders, sorted by label exactly as today; `semantic-diff.json`'s `headRules` is set to `[]` directly (never `JSON.parse(rulesRaw)`) — the branch belongs in `propose.mjs` (the caller), since `buildSemanticDiffReport()` already tolerates `headRules: []` via its own `?? []` defaults; `release-manifest.unsigned.json`, `conversion-report.json` (carrying the SAME named refusal reason P1-T2's `RuleEmissionRefusedError` would have named), and `semantic-diff.json` are ALL still written; `pack-provenance.json`/`evidence.json`/`evidence-assertions.json` (already written earlier in `run()`, before the write-gate) and the empty `rule-proposals.json`/`candidates.json` pair (P2-T7, FR-F11) are unaffected/unconditional either way. `run()` itself returns `EXIT_OK` (0) on a governance refusal — it does not throw. **Explicitly distinguish from `UnresolvedClaimReferenceError` (P1-T4)**: an invented/unresolvable claim id is a genuine content defect, NOT a "refusal" — that case is UNCHANGED by this task and continues to throw before any output is written (exit 2 SCHEMA), per FR-F7's existing wording. `batch.mjs`'s halt-on-first-failure contract for a genuine thrown `ConverterError` is otherwise UNCHANGED — this task's fix means a governance-refused pair simply no longer throws one. | A `node:test` case runs `propose` against a scratch copy of a module with zero `approved_for_rule_draft` decisions (reusing P1-T5's existing scratch-fixture technique — does not require Phase 2's module genericity to be testable) and asserts: the verb returns `EXIT_OK` (or the implementer's own explicitly-named non-crash code, itself asserted by the test, never left implicit); `evidence.json`, `evidence-assertions.json`, `pack-provenance.json`, `conversion-report.json` (carrying the named non-zero refusal reason), `semantic-diff.json`, `rule-proposals.json` (empty), and `candidates.json` (empty) all exist; `rules.json`/`rule-provenance.json` are absent (`fs.access` throws `ENOENT`). A second `node:test` (fixture-scoped, using the same scratch technique — a full real-fixture 4-pair run is re-verified once Phase 3 lands) asserts `batch` does not halt at a refused pair. `computeTraceabilityHash`'s empty-string substitution is unit-tested for determinism (two independent refused runs, byte-identical inputs, identical `traceabilityHashHex`). A regression check confirms `cbc_suite_v1`'s existing approved-emission path is unaffected — `rulesRaw`/`ruleProvenanceRaw` are still read from disk (never substituted) when `writeStagedRulesAndProvenance()` actually wrote them. | 1.5 | native | claude-sonnet-5 | extended | claude | P1-T2, P1-T3, P1-T4 |
| P1-T7 | Adversarial read-only diff review (Risk 1 mitigation, decisions block §6) | Per the decisions block's mandatory P1-P3 adversarial-review row and project memory (per-wave codex diff reviews have caught real fail-closed gaps validators approved in this repo before): a read-only `gpt-5.6-terra` review of P1-T1..T6, P1-T8's diff, hunting specifically for (1) any denylist-shaped branch masquerading as the allowlist gate, (2) any code path that reaches `writeStagedRulesAndProvenance()` without passing through the new `status` check, (3) any error path that swallows `RuleEmissionRefusedError`/`UnresolvedClaimReferenceError` before it reaches the CLI's top-level catch, (4) any place where P1-T8's non-fatal-refusal restructuring accidentally ALSO swallows `UnresolvedClaimReferenceError` (which must stay a hard crash, never non-fatal). This review **flags only** — findings are adjudicated by native Claude (P1-GATE), never auto-applied. | Review completes and produces a written finding list (even if empty); every finding is explicitly adjudicated (accepted-and-fixed, or explicitly rejected with rationale) before P1-GATE closes | 0.5 | gpt-5.6-terra | gpt-5.6-terra | medium | codex | P1-T1..T6, P1-T8 |
| P1-GATE | `task-completion-validator` + `karen` (safety-critical milestone) | Verify: gate is code-enforced (P1-T2/T3), not documentation; cross-resolution ships (P1-T4); a governance refusal is caught and non-fatal (P1-T8); negative control (P1-T5) and invariant (P1-T6) tests pass; P1-T7's adversarial findings are all adjudicated; `cbc_suite_v1`'s existing 4-rule emission path is provably unaffected. **`karen` specifically checks that no artifact anywhere in this phase's diff is described as "validated," "approved," or "release-ready."** | All exit-gate criteria pass; `karen` sign-off recorded in phase progress note | — | task-completion-validator, karen | claude-sonnet-5 | adaptive | claude | P1-T1..T8 |

**Phase 1 Quality Gates:**
- [ ] `propose.mjs` reads and branches on `pinned.decisions.parsed[].status` at runtime — grep-confirmed, not merely asserted
- [ ] The gate is coded as an allowlist (`status === 'approved_for_rule_draft'`); a test proves a 5th, unrecognized status value also refuses emission (the property a denylist cannot guarantee)
- [ ] `writeStagedRulesAndProvenance()` is conditional; `cbc_suite_v1`'s existing 4-rule emission is unaffected (P1-T3 seam task)
- [ ] Runtime `clm_*`/`evas_*` cross-resolution ships in this phase, not deferred (P1-T4)
- [ ] A governance refusal is a caught, non-fatal signal in `propose.mjs` — `conversion-report.json`/`semantic-diff.json`/evidence-layer artifacts are still written, `run()` returns `EXIT_OK`, and `batch` does not halt at a refused pair (P1-T8)
- [ ] Negative-control test (P1-T5) and repo-level invariant test (P1-T6) both pass
- [ ] P1-T7's adversarial review findings are all adjudicated before this gate closes
- [ ] `karen` confirms no artifact in this phase's diff overstates validation/approval status

[Return to Parent Plan](../multi-bundle-conversion-e1-finish.md)
