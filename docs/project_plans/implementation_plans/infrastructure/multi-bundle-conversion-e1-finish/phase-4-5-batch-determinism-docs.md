# Phase 4-5: Batch Determinism, Semantic-Diff & Docs

[Return to Parent Plan](../multi-bundle-conversion-e1-finish.md)

**Column conventions**: same as prior phase files. Phase 4's build/harness tasks route to
`codex-executor` (`gpt-5.6-terra`); its adjudication task (P4-T6) is **MUST-stay-primary**
(`native`, `claude-sonnet-5`) per this plan's binding rule that adjudication is never delegated, even
in an otherwise off-primary phase. Phase 5's docs/runbook/CHANGELOG tasks route to `ica-executor`
(`claude-haiku-4-5`); its design-spec and findings-doc tasks stay `native`.

**Executable-AC discipline**: identical to prior phase files.

---

## Phase 4: 4-of-4 Batch + Determinism + Semantic-Diff (R-3)

**Duration**: ~2-3 engineer-days
**Dependencies**: Phase 3 complete (all 3 non-cbc modules have their non-approving decisions files)
**Assigned Subagent(s)**: `codex-executor` (`gpt-5.6-terra`, determinism/semantic-diff harness —
escalates to `gpt-5.6-sol` on the two-failure rule); native Claude (`claude-sonnet-5`, adjudication of
the semantic-diff result only — MUST-stay-primary); `task-completion-validator` gate
**Exit gate** (decisions block §1, PRD Goal 4/FR-F14-F16): reproducibility claim is true for 4/4;
3 independent `batch` runs produce SHA-256-identical bytes for all 4 modules; `semantic-diff.json` is
produced AND committed for the 3 non-cbc modules — R-3 binding: it is NEVER used to overwrite
committed bespoke evidence in this pass, regardless of the diff's content.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Provider | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|----------|--------------|
| P4-T1 | Verify `batch` completes 4/4 `BATCH_PAIRS` live (FR-F14) | `tools/rf-bundle-to-kb-pack/lib/batch.mjs`'s `BATCH_PAIRS` array already lists all 4 named pairs (`rf-ev-001→anemia`, `rf-cbc-002→cbc_suite_v1`, `rf-kid-001→kidney_suite_v1`, `rf-gro-002→growth_suite_v1`, committed by the prior E1 pass) — previously only pair 1 succeeded (the other 3 halted at the module-identity `UsageError`). With Phases 1-3 landed, run `node cli.mjs batch` live and confirm all 4 pairs now complete `inspect → verify → propose` (the 3 non-cbc pairs are refused at the governance gate for rule content but still emit their evidence-layer artifacts per FR-F11's scope — "completes" means reaches a clean, named terminal state, not that every pair emits rules). | `node cli.mjs batch` exits 0; per-pair `conversion-report.json` exists for all 4 pairs; `cbc_suite_v1`'s pair shows 4 rules emitted (unchanged); the other 3 pairs show 0 rules emitted with a named governance refusal reason each | 1.0 | codex-executor | gpt-5.6-terra | medium | codex | Phase 3 complete |
| P4-T2 | `multi-bundle-conversion-report.json` — real 4-module aggregate (FR-F14) | Verify/extend `tools/rf-bundle-to-kb-pack/lib/multi-bundle-report.mjs`'s `aggregate` step now reports real counts for all 4 bundles (`bundlesReported: 4`, `bundlesNotAvailable: 0`) instead of the prior pass's `bundlesReported: 0`/`bundlesNotAvailable: 4`. Every per-bundle field (claims processed, conflicts, unresolved, rules emitted) has an explicit `0`/`[]` representation, never a missing key, per R-P2. | `tests/ef-converter-multi-bundle-report.test.mjs` passes with `bundlesReported: 4`; a test asserts a bundle producing zero rules still shows `"rulesEmitted": 0` (not an absent key) | 0.75 | codex-executor | gpt-5.6-terra | medium | codex | P4-T1 |
| P4-T3 | Triple-run determinism proof, all 4 bundles (FR-F15) | Extend `tests/ef-multi-bundle-determinism.test.mjs` to run `batch` 3 independent times against byte-identical fixture inputs and SHA-256-diff every emitted file, for all 4 bundles independently AND the aggregate `multi-bundle-conversion-report.json` — previously this harness could only meaningfully exercise `cbc_suite_v1`, since the other 3 bundles produced no output to compare. | Test passes; 3 runs produce byte-identical SHA-256 for every file across all 4 modules and the aggregate report; test fails loudly if any of the 4 modules is silently skipped from the comparison | 1.0 | codex-executor | gpt-5.6-terra | medium | codex | P4-T1 |
| P4-T4 | Extend `lib/semantic-diff.mjs` with an evidence-projection diff mode (R-3, FR-F16) | `lib/semantic-diff.mjs` today only supports a rule-`id`-level added/removed/changed comparison (the E0-era, OQ-4-scoped tool comparing `cbc_suite_v1`'s staged proposal against `modules/anemia/rules.json`) — this is new capability, not reuse. Add `diffEvidenceAssertions()`: a pure, deterministic, sorted-output function performing an added/removed/changed comparison, keyed by `assertionId`, between two `evidence-assertions.json` documents. Wire `propose`'s emission for `anemia`/`kidney_suite_v1`/`growth_suite_v1` to write `semantic-diff.json` into its `--out` tree, comparing its own freshly-produced `evidence-assertions.json` against that module's currently-committed one. | `diffEvidenceAssertions()` is a pure function (no I/O, no timestamps) with its own unit test asserting: comparing a document against itself always yields empty added/removed/changed; a seeded single-field change on one assertion yields exactly one `changed` entry naming that `assertionId`; `propose`'s `--out` tree for all 3 non-cbc modules contains a `semantic-diff.json` after this task | 2.0 | native | claude-sonnet-5 | extended | claude | P4-T1 |
| P4-T5 | Commit `semantic-diff.json` for `anemia`/`kidney_suite_v1`/`growth_suite_v1` (R-3, FR-F16 — **must produce AND commit, per this plan's binding instructions**) | Run `propose` for all 3 non-cbc modules and copy the resulting `semantic-diff.json` from `build/kb-pack/<id>/.../semantic-diff.json` (gitignored) into a durable, committed location: `modules/<id>/semantic-diff.json`, alongside that module's other converter-provenance artifacts (mirroring `cbc_suite_v1`'s own precedent of committing non-clinical provenance files like `traceability-index.json` alongside its clinical content). **The converter's freshly-produced `evidence.json`/`evidence-assertions.json` for these 3 modules stays in `build/` only and is NEVER copied over the committed `modules/<id>/evidence.json`/`evidence-assertions.json` — regardless of whether P4-T4's diff is empty or non-empty.** | `modules/{anemia,kidney_suite_v1,growth_suite_v1}/semantic-diff.json` are all committed; a test asserts `git diff` on `modules/{kidney_suite_v1,growth_suite_v1}/evidence.json` and `modules/anemia/evidence-assertions.json` is empty after this task runs (i.e., committing the diff never touched the committed evidence files it diffed against) | 0.5 | native | claude-sonnet-5 | adaptive | claude | P4-T4 |
| P4-T6 | FR-F16 closure-path decision (seam task, R-P3, **MUST-stay-primary adjudication**) | Read P4-T4/T5's actual, empirical semantic-diff result for each of the 3 modules and document the closure path per-module: **if a module's diff is empty**, `propose` (committed code, covered by `npm run check`) is documented (P5-T3) as the regenerator of record for that module's evidence layer going forward — the bespoke one-off generator stays committed as a historical/reference artifact, never deleted, but is no longer the sole regeneration path (closing finding #3's "not regenerable from committed code" gap, since `propose` now IS that committed, tested code). **If a module's diff is non-empty**, the committed bespoke evidence stays authoritative unchanged (R-3's fail-closed default), and the specific divergence is recorded as a new, named finding (feeding P5-T11's findings doc) rather than silently resolved either direction. This decision requires reading actual output, not assuming an outcome — it is adjudication, not mechanical work, hence MUST-stay-primary even though P4-T1..T5 route off-primary. | A short, explicit per-module closure statement exists (in this task's own commit message or a short note feeding P5-T3/P5-T11), citing the actual `added`/`removed`/`changed` counts from P4-T4's output for each of the 3 modules; the statement is conditional on the REAL result, not asserted before the diff was run | 0.75 | native | claude-sonnet-5 | adaptive | claude | P4-T5 |
| P4-GATE | `task-completion-validator` gate | Verify: batch 4/4 (P4-T1); aggregate report real (P4-T2); triple-run determinism holds for all 4 modules (P4-T3); `semantic-diff.json` committed for 3 modules (P4-T5) with committed bespoke evidence provably untouched; FR-F16 closure path documented per actual diff result (P4-T6); FR-F24's invariant test (P1-T6) re-confirmed green. | All exit-gate criteria pass; recorded in phase progress note | — | task-completion-validator | claude-sonnet-5 | adaptive | claude | P4-T1..T6 |

**Phase 4 Quality Gates:**
- [ ] `batch` completes `inspect → verify → propose` for all 4 named pairs; 0 rules for 3 of 4 modules, 4 rules (unchanged) for `cbc_suite_v1`
- [ ] 3 independent `batch` runs produce byte-identical output across all 4 modules and the aggregate report
- [ ] `semantic-diff.json` committed for `anemia`/`kidney_suite_v1`/`growth_suite_v1`, regardless of diff result
- [ ] Committed bespoke `evidence.json`/`evidence-assertions.json` for the 3 non-cbc modules is provably untouched by this phase (R-3)
- [ ] FR-F16's closure path is documented per-module, conditional on the actual empirical diff result
- [ ] FR-F24's invariant test re-confirmed green

---

## Phase 5: Honesty Reconciliation, Docs, Findings Closure

**Duration**: ~2 engineer-days
**Dependencies**: Phase 4 complete (the code's actual, tested behavior must be known before docs
describing it are written — writing docs earlier is how the prior pass's own P6-T3 AC/reality
mismatch happened)
**Assigned Subagent(s)**: `ica-executor` (`claude-haiku-4-5`, docs/runbook/CHANGELOG/tracker-resync —
mechanical, free-tier eligible); native Claude (`claude-sonnet-5`, design specs + findings doc —
architectural judgment content, not routed to haiku); `task-completion-validator` + `karen`
(end-of-feature review)
**Exit gate** (decisions block §1): no doc claims more than the code does; a reviewer can trace every
honesty claim to a test.

**File-disjointness proof for this phase's doc/spec batch** (all of P5-T3 through P5-T11 can run
concurrently once P5-T1/T2 close, since none share a file):

| Task | File(s) touched |
|---|---|
| P5-T3 | `docs/architecture.md` |
| P5-T4 | `tools/rf-bundle-to-kb-pack/README.md` |
| P5-T5 | `CHANGELOG.md` |
| P5-T6 | `CLAUDE.md` (verification only, no edit expected) |
| P5-T7 | `docs/project_plans/design-specs/df-e1-m1-rule-authoring-workflow.md` (new) |
| P5-T8 | `docs/project_plans/design-specs/df-e1-m3-anemia-reconciliation.md` (new) |
| P5-T9 | `docs/project_plans/design-specs/df-e1-m2-clinical-review-portal-intake.md` (new) |
| P5-T10 | `docs/project_plans/design-specs/df-ext-m1-legal-signoff-routing.md` (new) |
| P5-T11 | `.claude/findings/multi-bundle-conversion-e1-finish-findings.md` (new) |

All 9 file targets above are pairwise distinct — verified by direct enumeration, no intersection.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Provider | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|----------|--------------|
| P5-T1 | Verify FR-F18 (P1-T7 AC wording) already resolved | Prior pass's own findings doc (finding, "P1-T7's stated acceptance criterion overstates the rights-leakage gate's actual coverage") flagged `phase-1-2-vendoring-batch-orchestration.md`'s row P1-T7 as overstating coverage. Direct inspection (this plan's own research) confirms that row's current committed text ALREADY states the real, scoped property ("This is not byte-level absence proof... Scope limits, stated here deliberately") — this was fixed in commit `263120b`, already merged. This task is a verification-only pass: grep the repo for any RESIDUAL occurrence of the overstated "greps every committed byte" claim as a positive assertion (docs, README, tool headers) and fix any found; if none found, record the finding as already-closed with the fixing commit cited. | `grep -rn "greps every committed byte"` (or equivalent overstated phrasing) across `docs/`, `tools/`, `scripts/`, `README.md` returns zero positive-claim occurrences (a negative/scoped mention like this task's own description is fine); P5-T11's findings doc cites commit `263120b` as the fix for finding #4 | 0.25 | ica-executor | claude-haiku-4-5 | low | ICA free | Phase 4 complete |
| P5-T2 | Resync prior plan's stale progress-tracking artifacts (FR-F19) | Inspect `.claude/progress/multi-bundle-conversion-e1/` for any per-phase file still reading `in_progress`/`not_started` for phases whose deliverables are committed (decisions block Risk 6). Mark accurate, or mark the tracker explicitly superseded by this plan's own `.claude/progress/multi-bundle-conversion-e1-finish/` tracker — do not leave two contradictory trackers for the same converter surface. | Every file under `.claude/progress/multi-bundle-conversion-e1/` reflects `completed` status (matching the prior plan's own frontmatter `status: completed`) or carries an explicit "superseded by multi-bundle-conversion-e1-finish" note | 0.5 | ica-executor | claude-haiku-4-5 | low | ICA free | Phase 4 complete |
| P5-T3 | Update `docs/architecture.md` §2a module inventory table | Reflect the converter's actual, current genericity: all 4 modules can now reach `propose`'s emission gate; `cbc_suite_v1` remains the sole rule-bearing module (SHA-256 byte-identical throughout this pass); `anemia`/`kidney_suite_v1`/`growth_suite_v1` each now have a non-approving `authoring-decisions.yaml`; document P4-T6's per-module semantic-diff closure-path result (converter-derived-going-forward vs. bespoke-stays-authoritative) using the ACTUAL result, not a template placeholder. | `docs/architecture.md` §2a's table is updated; every claim in the updated section traces to a test or a committed artifact from Phases 0-4 (not asserted from memory); the FR-22/FR-F5 supersession relationship (OQ-5) is cross-referenced explicitly | 0.75 | ica-executor | claude-haiku-4-5 | low | ICA free | Phase 4 complete, P4-T6 |
| P5-T4 | Update the converter runbook (`tools/rf-bundle-to-kb-pack/README.md`) | Document the new fail-closed allowlist gate (R-2), the module-generic drafting registry (OQ-2), and the runtime `clm_*`/`evas_*` cross-resolution guard (FR-F7) as part of the verb documentation for `propose`. | README's `propose` section describes the allowlist gate, the registry, and the cross-resolution guard; no claim of clinical validation or release-readiness appears anywhere in the update | 0.5 | ica-executor | claude-haiku-4-5 | low | ICA free | Phase 4 complete |
| P5-T5 | `CHANGELOG.md` `[Unreleased]` entry (FR-F21) | Add an entry describing: the code-enforced fail-closed allowlist gate; the module-generic converter; the 3 new non-approving decisions files; the 4-of-4 batch/determinism proof; the committed `semantic-diff.json` per non-cbc module; the explicit "zero new clinical rules" outcome. Never described as a content release or a step toward one — matches this program's existing CHANGELOG framing precedent (the SPA Module Switcher entry's own "unvalidated research prototype" framing). | Entry exists under `[Unreleased]`, correctly categorized per `.claude/specs/changelog-spec.md`; explicitly states zero new clinical rules and that this remains an unvalidated research prototype | 0.25 | ica-executor | claude-haiku-4-5 | low | ICA free | Phase 4 complete |
| P5-T6 | Verify `CLAUDE.md`/check-gate final consistency (FR-F20/OQ-6) | Confirm `tests/claudemd-check-gate.test.mjs` is still green after all of P0-P4's changes (P0-T8 already synced the reordered `scripts.check` string same-commit; this task is a final, whole-plan re-verification, not a new edit). Confirm no later phase introduced a further check-gate string change (batch already lists all 4 pairs; no new `npm run check` step was added by Phases 1-4). | `tests/claudemd-check-gate.test.mjs` passes; `git diff` on `package.json`'s `scripts.check` between the start of P0 and the end of P4 shows exactly the one reorder from P0-T8, no further drift | 0.25 | ica-executor | claude-haiku-4-5 | low | ICA free | Phase 4 complete |
| P5-T7 | Create `df-e1-m1-rule-authoring-workflow.md` design spec (FR-F22, R-1 — **create, not update**) | Author a NEW design spec (`maturity: shaping`) describing the deferred rule-authoring promotion workflow (how a decision's `status` gets promoted to `approved_for_rule_draft` for any of the 4 modules). Cross-reference the differently-named prior artifact `rule-authoring-workflow-per-module.md` in `related_documents` — do not duplicate its content, and do not edit that file in place of creating this one (R-1 is explicit that this path does not exist and must be created). | `docs/project_plans/design-specs/df-e1-m1-rule-authoring-workflow.md` is a NEW file (git-confirmed); frontmatter sets `prd_ref` to this feature's PRD; `related_documents` cross-references the prior, differently-named spec | 0.75 | native | claude-sonnet-5 | adaptive | claude | Phase 4 complete |
| P5-T8 | Create `df-e1-m3-anemia-reconciliation.md` design spec (FR-F22, R-1 — **create, not update**) | Author a NEW design spec describing the anemia evidence-layer reconciliation question between the EP-3/EP-4 pipeline's `evidence.json` and the converter's `evidence-assertions.json`/`semantic-diff.json` output — now informed by P4-T4/T5/T6's actual empirical semantic-diff result for `anemia`, not a hypothetical. Cross-reference `anemia-backfill-reconciliation-procedure.md`. | New file, git-confirmed; incorporates the real P4 semantic-diff result for anemia (cite the actual added/removed/changed counts, not a placeholder) | 0.75 | native | claude-sonnet-5 | adaptive | claude | Phase 4 complete, P4-T6 |
| P5-T9 | Create `df-e1-m2-clinical-review-portal-intake.md` design spec (FR-F22, R-1 — **create**) | Author a NEW design spec for the deferred clinical-review-portal intake of this pass's conflict objects / `rule-proposals.json` / `unresolved.json` / `semantic-diff.json`. Cross-reference `clinical-review-portal-intake-e1-artifacts.md`. | New file, git-confirmed; `prd_ref` set to this feature's PRD | 0.5 | native | claude-sonnet-5 | adaptive | claude | Phase 4 complete |
| P5-T10 | Create `df-ext-m1-legal-signoff-routing.md` design spec (FR-F22, R-1 — **create**) | Author a NEW design spec for the deferred legal sign-off routing mechanism covering the 35 P0-backfilled sources' (and every pre-existing `UNKNOWN`-status source's) rights determinations. Cross-reference `reg-001-004-legal-signoff-routing.md`. Describes the ROUTING MECHANISM only — never the determination itself; `scripts/rights/build-decision-brief.mjs` is the existing tool this spec routes toward. | New file, git-confirmed; explicitly states no rights determination is made or implied by this spec itself | 0.5 | native | claude-sonnet-5 | adaptive | claude | Phase 4 complete |
| P5-T11 | Author findings doc, closing prior findings #1/#3/#4 (FR-F23) | Create `.claude/findings/multi-bundle-conversion-e1-finish-findings.md` (lazy-created per this program's lifecycle — set this plan's `findings_doc_ref` at this point if not already set by an earlier in-flight finding). Explicitly close out the prior pass's 3 tracked findings: #1 (shared-mutable-state test hazard) → cross-reference the P2-T5 fixing commit; #3 (unreproducible-provenance gap) → cross-reference P4-T6's per-module closure-path decision and its fixing commits; #4 (P1-T7 AC overstatement) → cross-reference commit `263120b` (already-fixed, per P5-T1). Record any NEW finding surfaced during this pass's own execution (if any). | Findings doc exists, `status: accepted`; all 3 prior findings marked closed with commit cross-references; if any new finding was captured during P0-P4, it is recorded here with its own resolution status | 1.0 | native | claude-sonnet-5 | adaptive | claude | P5-T1..T10 |
| P5-GATE | `task-completion-validator` + `karen` (end-of-feature review) | Verify: all 4 design specs created (not updated); findings doc closes all 3 prior findings; CHANGELOG/README/architecture docs updated; `CLAUDE.md`/check-gate consistency holds; every hard guardrail in `CLAUDE.md` and every PRD §7 non-goal explicitly checked against this pass's actual diff, not assumed compliant. **`karen` performs the final, whole-feature check that no artifact anywhere in this plan's total diff is described as clinically validated, approved, or release-ready**, and that `approvedBy[]`/`clinicalApprovers[]` remain schema-forced empty on all 4 modules. | All exit-gate criteria pass; `karen` end-of-feature sign-off recorded | — | task-completion-validator, karen | claude-sonnet-5 | adaptive | claude | P5-T1..T11 |

**Phase 5 Quality Gates:**
- [ ] All 4 Deferred Items rows have a NEWLY CREATED design spec (R-1); `deferred_items_spec_refs` frontmatter matches
- [ ] `findings_doc_ref` is populated (or explicitly stays `null` if no new in-flight finding occurred) and, if populated, `status: accepted`
- [ ] Prior findings #1, #3, #4 are explicitly retired with fixing-commit cross-references
- [ ] `CHANGELOG.md` `[Unreleased]` entry present; `docs/architecture.md` §2a updated; converter README updated
- [ ] `tests/claudemd-check-gate.test.mjs` green; no further check-gate string drift beyond P0-T8's one reorder
- [ ] `karen` end-of-feature review confirms zero overstated validation/approval claim anywhere in this plan's total diff

[Return to Parent Plan](../multi-bundle-conversion-e1-finish.md)
