# Phases 2–4: Review · Release · Retro Workstreams (parallel wave 2)

[Return to Parent Plan](../evidence-foundry-e1-v1.md)

**Column conventions**: `Estimate` in points, never Effort. `Model`: `sonnet` \| `haiku`. `Effort`
(claude only): `adaptive` \| `extended`. Gate rows carry `Estimate: —`.

**Wave note**: P2 ∥ P3 ∥ P4 open together after P1-GATE2 (karen). File ownership is disjoint:
`tools/review-record/` + `modules/cbc_suite_v1/reviews/` (P2) vs `tools/release-sign/` +
`releases/` + `scripts/validate-kb.mjs` (P3, sole barrier-file owner in this wave) vs
`tools/retro-validate/` (P4, tool-local schemas only). Each phase gates independently.

**Honesty guardrail (every task)**: no output states or implies clinical validity, safety,
diagnostic performance, or regulatory status (FR-28). All metrics are software-agreement measures.
No task clears or depends on gates G0–G4; gated behaviors ship schema-forced inert. Every CLI is
offline, deterministic, zero-network, zero-generative-model (NFR; test-enforced per tool).

---

## Phase 2: Review Workflow Machinery

**Duration**: ~4-5 engineer-days
**Dependencies**: Phase 1 complete (P1-GATE2 passed)
**Assigned Subagent(s)**: general-purpose executor (Node CLI/store/validators),
documentation-writer (banner + non-qualifying copy in P2-T6/T8); task-completion-validator gate
**Exit gate** (decisions block §1): full five-role cycle executable with synthetic identities
clearly marked non-credentialed; append-only enforced by tests; render output carries the
unvalidated-prototype banner; `npm run check` + validator.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|---------------|
| P2-T1 | `tools/review-record/` CLI scaffold + store layout (OQ-1/OQ-2) | Per FR-1/FR-7, OQ-1/OQ-2 resolutions: scaffold `tools/review-record/` as a Node ESM CLI (`cli.mjs` verb dispatch: `scaffold` \| `validate` \| `list` \| `render` \| `dry-run`; pinned YAML parser + JSON Schema validator deps matching E0's converter choices; README documenting the internal module boundary — store / chain / roster / signature / render — that P2-T2..T6 build against). Implement the OQ-2 store layout: records at `modules/<module_id>/reviews/rr-<seq4>-<role>.yaml`; `list` prints per-module review state (records by role, chain validity, synthetic flags). Offline, deterministic, no network, no generative model (FR-7). | `node tools/review-record/cli.mjs --help` lists all 5 verbs (`render`/`dry-run` may exit "not yet implemented" until P2-T6/T8); `list` over a fixture module prints a structured non-empty state summary; README module boundary present; a test asserts zero network calls and zero model-invocation hooks across all verbs | 1.0 | general-purpose | sonnet | adaptive | P1-GATE2 |
| P2-T2 | Role scaffolding + roster checks + reviewer-2 independence | Per FR-3/FR-4/FR-7: `scaffold --module <id> --role <role> --subject <content-hash>` creates a schema-valid draft record for each of the 5 roles, requiring `reviewerId` to resolve against `governance/reviewer-roster.yaml` (unknown identity → fail-closed). **Reviewer-2 independence (FR-4), enforced structurally**: the `clinical-2` scaffold flow must not read, print, or embed any `clinical-1` decision content — the scaffolded `clinical-2` record has no field referencing reviewer-1's vote/rationale, and the CLI refuses `scaffold --role clinical-2` output that would include it. Seeded-violation tests: (a) a hand-built `clinical-2` record referencing reviewer-1 decision content is rejected by `validate`; (b) a record citing a non-roster `reviewerId` is rejected. | All 5 roles scaffold to schema-valid drafts; both seeded violations rejected with specific errors; a test inspects `scaffold --role clinical-2` output and asserts zero reviewer-1 content present; non-roster identity fail-closed | 1.25 | general-purpose | sonnet | adaptive | P2-T1 |
| P2-T3 | Append-only enforcement (hash chain + git history) | Per FR-9, OQ-2: implement the two-layer append-only validator — (a) `previousRecordHash` chain: each record after the first for a module carries the SHA-256 of the prior record's canonical bytes; `validate` recomputes and fails closed on any break; (b) git-history check: a validator (invoked by `validate --history`) rejects any commit-visible mutation or deletion of an existing `modules/<id>/reviews/*.yaml` path — corrections must be new superseding records (`supersedes` field). Seeded-mutation tests: mutate one byte of a committed record fixture → chain validator fails; simulate a history rewrite in a scratch git fixture → history validator fails. Chain state is a structured audit artifact (NFR observability). | Both seeded mutations rejected fail-closed with distinct errors; a valid superseding record passes while an in-place edit fails; chain recomputation is deterministic (two runs byte-identical output) | 1.25 | general-purpose | sonnet | extended | P2-T2 |
| P2-T4 | Adjudication + release-authorization validators (PRD OQ-5) | Per FR-5/FR-6: implement the authorship-union computation (PRD OQ-5 resolution: identities in `authoring-decisions.yaml` decision records ∪ git author of the proposal-introducing commit; the converter is never an identity) as a machine-readable `authorship` block, and enforce **adjudicator ∉ authorship-union** in `validate`. Release-authorization chain validation: a `release-auth` record is valid only over a complete, chain-valid, roster-verified, **non-synthetic** record set — and even then E1 raises no ceiling: the `unsigned-stub → release-ready` manifest transition stays schema-impossible (P1-T7's enforcement re-asserted here by test, not weakened). Seeded-violation tests: adjudicator = an authorship-union identity → rejected; a `release-auth` record over a chain containing any `synthetic: true` record → structurally non-qualifying. | Authorship union computed correctly for the cbc_suite_v1 fixture pack (both sources represented); both seeded violations rejected; a test proves no code path in this tool can set `release-ready` or populate `approvedBy[]`/`clinicalApprovers[]` | 1.0 | general-purpose | sonnet | adaptive | P2-T2 |
| P2-T5 | Record signature binding (test keys only) | Per FR-10, OQ-2/OQ-6: implement the record `signature` object — Ed25519 (`node:crypto` only) over the canonicalized record bytes minus the signature object, binding `reviewerId` to `subjectContentHash`. E1 signing exists **only** in synthetic dry-run mode: ephemeral in-memory keypair per invocation (OQ-6 — never written to disk, no `--test-keys` flag), `keyId` forced to the `TESTKEY-` prefix, and only writable onto `synthetic: true` records (schema already forbids it elsewhere, P1-T2). `validate` verifies present signatures and fails closed on tamper. | Sign→verify roundtrip green on a synthetic record; one-byte tamper of the signed body fails verification; a test asserts no file matching private-key patterns is created anywhere during the full test run; attempting to sign a `synthetic: false` record fails closed | 1.0 | general-purpose | sonnet | adaptive | P2-T3 |
| P2-T6 | Read-only static render (FR-8/FR-31, OQ-3) | Per FR-8/FR-31, OQ-3: `render --module <id> [--record <review_id>]` emits self-contained static HTML to `build/review-render/` (gitignored) showing the passage → decision → rule → test chain for a record under review, from committed artifacts only. Explicitly NOT a portal: no server, no database, no write path, no auth, no scripts, no third-party/remote assets. Every page carries the unvalidated-research-prototype banner (copy authored by documentation-writer) and per-record non-qualifying labels for synthetic content. **Rights posture (FR-31)**: rights-restricted passages render as hash + selector reference blocks, never inline text (E0 OQ-2 precedent, ADR-0002). Commit one golden render under `tests/fixtures/ef-review-render/`. | Render of the P2-T8 dry-run chain produces valid self-contained HTML; grep-test proves zero `<script`, zero external URL references; banner string present on every emitted page; a rights-restricted fixture passage appears only as hash+selector, never text; golden matches byte-for-byte on re-render (deterministic) | 1.0 | general-purpose, documentation-writer | sonnet | adaptive | P2-T4 |
| P2-T7 | Render runtime smoke (R-P4) | R-P4 smoke task for the only UI-adjacent surface: `tests/ef-review-render-smoke.test.mjs` renders from committed fixture artifacts end to end and asserts: output file exists and is non-empty well-formed HTML; the unvalidated-prototype banner and synthetic non-qualifying labels are present; zero network calls occur during render (test-enforced); output under `build/` is ignored by git (E0 P1-T7 precedent). | Smoke test green in `npm test`; all four assertions explicit and individually named in the test | 0.5 | general-purpose | sonnet | adaptive | P2-T6 |
| P2-T8 | Five-role synthetic dry-run (FR-11) | Per FR-11 (ruling R4): execute one full end-to-end dry-run via `cli.mjs dry-run` — scaffold → sign (TESTKEY, P2-T5) → chain-validate all five roles in sequence (`clinical-1`, `clinical-2`, `lab`, `adjudication`, `release-auth`) over the cbc_suite_v1 proposal's `subjectContentHash`, using labeled synthetic personas (roster entries `synthetic: true`, names like "SYNTHETIC — NOT A CREDENTIALED REVIEWER"). Commit the record set to `modules/cbc_suite_v1/reviews/` (they are structurally synthetic and non-qualifying by schema) with goldens under `tests/fixtures/`. Every artifact carries explicit non-qualifying language; the dry-run populates **zero** approver/`clinicalApprovers` fields (schema-proven). Emit a short friction-observations note (`.claude/worknotes/evidence-foundry-e1-v1/dryrun-friction.md`) — the first evidence feed for PRD OQ-8's portal trigger (human decision, not this plan's). | 5/5 record types committed, chain-valid, each `synthetic: true` with non-qualifying language; `npm run validate` green over the committed set; schema test proves zero approver fields populated anywhere; friction note committed (observations only, no portal recommendation) | 1.0 | general-purpose, documentation-writer | sonnet | adaptive | P2-T5, P2-T6 |
| P2-GATE | `task-completion-validator` gate | Verify Phase 2 exit gate: five-role dry-run committed and chain-valid; both append-only layers reject seeded mutations; reviewer-2 independence and adjudicator≠author violations rejected; render smoke green with banner; zero approver fields populated; `npm run check` green; ADR-delta check (ADR-0004 unchanged, else escalate). | All exit-gate criteria pass; recorded in phase progress note | — | task-completion-validator | sonnet | adaptive | P2-T1..T8 |

**Phase 2 Quality Gates:**
- [ ] `npm run check` green (quality gate: `npm run check` + task-completion-validator)
- [ ] Full five-role cycle executable; all synthetic artifacts structurally marked non-credentialed
- [ ] Seeded violations (mutation ×2, reviewer-2 dependence, adjudicator=author, non-roster identity) — 5/5 rejected fail-closed
- [ ] Render self-contained, banner-stamped, rights-respecting; smoke test green
- [ ] Zero approver/signature fields populated on any non-synthetic artifact (schema-proven)

---

## Phase 3: Signed Release Machinery

**Duration**: ~4-5 engineer-days
**Dependencies**: Phase 1 complete (P1-GATE2 passed); independent of P2/P4
**Assigned Subagent(s)**: general-purpose executor (Node crypto; decisions block §6: correctness
over speed — `extended` on crypto/determinism tasks); documentation-writer (P3-T7 runbook);
task-completion-validator gate
**Seam note** (R-P3): P3-T6 is the sole post-P1 change to `scripts/validate-kb.mjs` (serialization
barrier) — no other wave-2 phase touches it.
**Exit gate** (decisions block §1): deterministic manifest reproducible byte-for-byte; CI verifies
but can never sign; test asserts no key material in repo/CI env; `npm run check` + validator.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|---------------|
| P3-T1 | `tools/release-sign/` scaffold + E0 golden-bytes pin (FR-12 preimage) | Per FR-12, decisions block Risk 6: scaffold `tools/release-sign/` (Node ESM `cli.mjs`, verbs `manifest` \| `register` \| `sign` \| `verify`; `node:crypto` only — zero new crypto deps; README module boundary: canonical-bytes / manifest / registry / sign / verify). Pin a golden-bytes regression fixture generated from E0's P5-T5 canonical serialization of the cbc_suite_v1 pack under `tests/fixtures/ef-release/golden-canonical-bytes/`; the `manifest` verb builds the release-candidate hash manifest by **calling E0's existing canonicalization** (import from `tools/rf-bundle-to-kb-pack/`, never re-implement). A byte-identity test asserts the signing preimage equals E0's canonical bytes for the same pack; golden drift **fails the phase** — never silently re-baseline. | `--help` lists 4 verbs; byte-identity test green (preimage === E0 canonical bytes, SHA-256 equal); golden-drift test fails closed on a seeded one-byte change; zero non-`node:crypto` crypto imports (grep-test); zero network/generative-model calls (test) | 1.5 | general-purpose | sonnet | extended | P1-GATE2 |
| P3-T2 | Ed25519 `sign` verb — human-offline design, dry-run only in E1 (OQ-6) | Per FR-12/FR-15 (ruling R3): implement detached Ed25519 signing over the P3-T1 manifest digest. The verb is **designed for human offline execution** (reads a key from an operator-supplied path outside the repo at ceremony time — G2, never exercised in E1) and carries a `--dry-run` mode per OQ-6: ephemeral in-memory keypair, `keyId` forced to `TESTKEY-` prefix, private key discarded at process exit, output marked structurally as dry-run. No key generation verb writes anything to the tree; no automated check invokes `sign` outside dry-run. Never bypass or weaken the schema-forced-empty signature slot on real candidates (P1-T5). | Dry-run sign→verify roundtrip green and byte-stable across 2 runs (hash comparison); a test asserts no private-key material exists in the repo tree or process env after a full test run; non-dry-run `sign` without an operator key path fails closed with a G2-referencing error (and is never invoked by tests beyond this failure assertion) | 1.5 | general-purpose | sonnet | extended | P3-T1 |
| P3-T3 | `verify` verb — fail-closed exit-code taxonomy (FR-13) | Per FR-13: `verify --candidate <manifest> --registry releases/registry.json` is fail-closed with a documented exit-code taxonomy (README table): 0 ok · distinct non-zero codes for each of the 5 enumerated failure classes — (1) byte drift vs canonical bytes, (2) digest mismatch vs manifest, (3) unknown `keyId`, (4) registry inconsistency (entry missing/duplicated/digest disagreement), (5) `TESTKEY-` identity on a non-dry-run candidate. Non-zero exit → no partial output. Seeded tamper fixtures for all 5 classes. Verify-only is the CI/agent-reachable surface — CI can never sign (R3). | 5/5 seeded failure classes produce their distinct documented exit codes with zero partial output; verify over the P3-T2 dry-run candidate exits 0; exit-code table in README matches implementation (test-enforced) | 1.5 | general-purpose | sonnet | extended | P3-T2 |
| P3-T4 | Registry seed + append-only validator (FR-14, OQ-4) | Per FR-14, OQ-4: create `releases/registry.json` (top-level `schemaVersion` + empty `entries[]`) validating against P1-T5's schema; implement `register` — appends an entry (dry-run candidates carry the structural dry-run marker; real entries have `signature: null` pre-G2) and rejects any mutation/removal of existing entries (append-only, git-tracked; same two-layer approach as P2-T3 where applicable). E1 never sets `withdrawalState` ≠ `"none"` (validator-enforced const). | Registry seed validates; `register` appends deterministically (sorted, canonical serialization); seeded entry-mutation and entry-removal fixtures rejected fail-closed; a `withdrawalState: "withdrawn"` write attempt fails closed | 1.0 | general-purpose | sonnet | adaptive | P3-T3 |
| P3-T5 | No-keys + forced-empty enforcement tests (FR-15/FR-16) | Per FR-15/FR-16 (R3, SPIKE-006 reconciliation): `tests/ef-release-no-keys.test.mjs` — (a) scans the repo tree for private-key material patterns (PEM/OpenSSH/PKCS8 headers, raw Ed25519 seed files) and fails on any hit outside an explicit empty allowlist; (b) asserts no automated check, script, or CLI default reads a signing key from repo or env (grep + execution assertions); (c) proves a populated `signature` on a real (non-dry-run) candidate fails `npm run validate` (FR-16, via P1-T5/T7 schemas); (d) proves a `TESTKEY-` keyId in a real registry entry is rejected (release-path test-key leak, Risk "test keys leak"). | All 4 assertion groups green and individually named; test runs inside `npm test` (flat `tests/ef-*.test.mjs` glob, unmodified) | 1.0 | general-purpose | sonnet | adaptive | P3-T2, P3-T4 |
| P3-T6 | Verifier-surface wiring (FR-18, PRD OQ-2 — seam task) | Per FR-18, PRD OQ-2 resolution: structural verification joins `scripts/validate-kb.mjs` (registry schema-validity + append-only shape + forced-empty/TESTKEY checks run in `npm run validate`); full cryptographic `verify` remains a `tools/release-sign` verb exercised by tests — **not** wired into the SPA/API runtime and **not** a new npm script. The anemia browser deployment's SPIKE-006 posture (two-part digest, fail-closed, `unsigned-stub → integrity-recorded → superseded/revoked` enum) stays byte-untouched — assert via existing tests plus an explicit diff-scope check. Sole post-P1 barrier-file change; document the surface decision in the tool README. | `npm run validate` fails on a seeded bad registry; no change to `src/`, `server.mjs`, `openapi.yaml`, or any anemia-path digest/status file (diff-scope test); README records the OQ-2 surface decision and its rationale | 0.75 | general-purpose, backend-architect | sonnet | adaptive | P3-T4, P3-T5 |
| P3-T7 | Signing-ceremony runbook (FR-17) | Per FR-17: author `docs/governance/signing-ceremony-runbook.md` — human-executed offline key generation, custody model, signing steps over the canonical digest, rotation and compromise-response ownership, and the G2 entry criteria (custodian named, **distinct authority from the release author** per the A2 reconciliation; cross-reference the P1-T6 gates registry). A document deliverable only — the ceremony itself is gate G2, out of scope, and the runbook must say so explicitly. Carries the unvalidated-research-prototype posture; states that no signature confers clinical standing. | Runbook exists with all 5 sections (keygen, custody, signing, rotation/compromise, G2 criteria); A2 distinct-authority condition stated; explicit "ceremony = G2 human act, not performed by this plan" statement; honesty posture present | 0.75 | documentation-writer | sonnet | adaptive | P3-T2 |
| P3-GATE | `task-completion-validator` gate | Verify Phase 3 exit gate: byte-identity + golden-bytes tests green; dry-run sign→verify byte-stable across 2 runs; 5/5 verify failure classes fail closed; registry seeded, append-only, withdrawal inert; no-keys test green (4/4 groups); browser posture untouched; runbook complete; `npm run check` green; ADR-delta check (ADR-0005 unchanged, else escalate). | All exit-gate criteria pass; recorded in phase progress note | — | task-completion-validator | sonnet | adaptive | P3-T1..T7 |

**Phase 3 Quality Gates:**
- [ ] `npm run check` green (quality gate: `npm run check` + task-completion-validator)
- [ ] Signing preimage byte-identical to E0 P5-T5 canonical bytes (test-proven); golden drift fails closed
- [ ] CI/agents can verify but structurally cannot sign; zero key material in repo/CI/agent context (test-proven)
- [ ] Signature slot schema-forced empty on every real candidate; TESTKEY leak onto release path rejected
- [ ] Registry append-only, withdrawal-state inert; anemia browser SPIKE-006 posture byte-untouched

---

## Phase 4: Retrospective Validation Harness

**Duration**: ~4-5 engineer-days
**Dependencies**: Phase 1 complete (P1-GATE2 passed). P4-T3's registry-digest pinning consumes the
`releases/registry.json` **schema** (P1-T5); for execution it pins against a dry-run registry
fixture under `tests/fixtures/ef-retro/`, so P4 does not wait on P3 (wave-parallel safe).
**Assigned Subagent(s)**: general-purpose executor (harness); spike-writer (P4-T9 charter);
task-completion-validator gate
**Exit gate** (decisions block §1): harness refuses input not marked synthetic/de-identified;
metrics labeled software-agreement (never clinical performance); SPIKE charter committed;
`npm run check` + validator.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|---------------|
| P4-T1 | `tools/retro-validate/` scaffold + fixture-corpus schema (FR-20) | Per FR-19/FR-20 (ruling R6): scaffold `tools/retro-validate/` (Node ESM `cli.mjs`, verbs `check-fixtures` \| `run` \| `report`; README module boundary: corpus / boundary / replay / metrics / access-log). Author the tool-local fixture-corpus schema (`tools/retro-validate/schemas/fixture-corpus.schema.json` — tool-local by design so no wave-2 phase touches `scripts/validate-kb.mjs`): every case requires a `provenance` marker ∈ {`synthetic`, `deidentified`} with corpus-level source attestation ref; the schema **forbids identifier fields** (name, MRN, DOB, address, contact, SSN-like patterns — enumerated field-name + pattern denylist, `additionalProperties` constrained). E1 corpus content: synthetic + de-identified only. | Schema rejects a case missing `provenance` (R-P2) and rejects each of the ≥6 enumerated identifier field classes (one seeded fixture per class); a valid synthetic corpus fixture passes `check-fixtures`; zero network/generative-model calls (test) | 1.0 | general-purpose | sonnet | adaptive | P1-GATE2 |
| P4-T2 | Boundary enforcement validator (FR-20) | Per FR-20 (ADR-0006 binding clause): `check-fixtures` is the structural gate every other verb calls first — it rejects fail-closed (non-zero exit, no partial output) any corpus containing an identifier-bearing case, a case lacking a provenance marker, or a corpus without source attestation. `run`/`report` refuse to start on an unchecked or failing corpus. Seeded fixtures for all three rejection classes. This is the schema-enforced (not procedural) de-identification boundary: no patient-identifiable data can enter the repo, build outputs, or any downstream artifact through this tool. | 3/3 seeded rejection classes fail closed with distinct errors before any replay work begins; `run` on a failing corpus produces zero output files; boundary check is provably invoked by `run` and `report` (test asserts call order) | 1.0 | general-purpose | sonnet | adaptive | P4-T1 |
| P4-T3 | Version-pinned deterministic replay runner (FR-19) | Per FR-19: `run --corpus <dir> --candidate-digest <registry digest> --registry <path>` replays every corpus case through the engine build identified by the pinned registry digest — never "current tree": the runner resolves the candidate via the registry entry (dry-run registry fixture in E1) and fails closed on digest mismatch. Deterministic: canonical serialization, sorted case order, no timestamps in hashed bytes; two runs over identical corpus + digest produce **byte-identical** metric artifacts (hash-comparison test, FR-19). | Double-run determinism test green (byte-identical `agreement-report.json`); seeded digest-mismatch and absent-registry-entry runs fail closed with zero partial output; "current tree" execution is impossible (no code path accepts an unpinned candidate — test-asserted) | 1.5 | general-purpose | sonnet | extended | P4-T2 |
| P4-T4 | Software-agreement metrics report (FR-21, OQ-5) | Per FR-21, OQ-5 resolution: `report` emits `agreement-report.json` with exactly the 5 OQ-5 measures — case-level exact-agreement rate; per-candidate-pattern agreement/disagreement counts; dangerous-miss discordance count; safety-flag agreement coverage; missing-data-prompt agreement rate — every metric name carrying the `software agreement` label, and the report header carrying: the unvalidated-prototype banner, "software-agreement measures against fixture reference labels — NOT clinical performance, sensitivity, or specificity", and the FR-24 non-qualifying protocol banner when the protocol is unpopulated (always, in E1). Provenance sidecar `run-provenance.json` (corpus id, harness version, candidate registry digest, run timestamp) — the sole timestamp location, outside the determinism-compared bytes. Only these de-identified aggregates ever cross into repo artifacts. | Report contains exactly the 5 named metrics, each labeled software-agreement; grep-test proves the strings `sensitivity`, `specificity`, `clinical performance` appear nowhere in report output except inside the explicit negation banner; provenance sidecar complete per FR-21; report bytes deterministic (P4-T3 test covers) | 1.0 | general-purpose | sonnet | adaptive | P4-T3 |
| P4-T5 | Discordance / adjudication record model (FR-23) | Per FR-23: each harness-vs-reference-label disagreement emits an adjudication-ready discordance record (tool-local schema: case ref, candidate digest, engine output set, reference label set, disagreement class) structurally consumable by Workstream A — its shape maps onto a P1-T2 canonical `adjudication`-role scaffold input, and the adjudicator-≠-author check reuses the PRD OQ-5 authorship-union definition (shared helper, not re-implemented). Missing-field fixture rejected (R-P2). | A seeded discordant corpus yields discordance records that `tools/review-record scaffold --role adjudication` accepts as subject input (integration test); authorship-union helper shared (import, not copy — grep-test); missing-field fixture rejected | 1.0 | general-purpose | sonnet | adaptive | P4-T4 |
| P4-T6 | Prespecified-protocol shape — human-only thresholds (FR-24) | Per FR-24: author the protocol schema (`tools/retro-validate/schemas/protocol.schema.json`) with slots for dangerous-miss rate, utility measures, and subgroup/analyzer/site strata — every threshold field `null`/TBD-by-named-humans (const null in E1; the software never invents or defaults a clinical threshold). An unpopulated protocol renders every harness report "non-qualifying — protocol not prespecified by humans" (wired in P4-T4). A seeded protocol fixture with a software-populated threshold is rejected fail-closed. | Protocol schema validates the null-threshold shape; seeded populated-threshold fixture rejected; report banner proven present when protocol unpopulated; missing-field fixture rejected (R-P2) | 0.75 | general-purpose | sonnet | adaptive | P4-T4 |
| P4-T7 | Validation-data access log (FR-22) | Per FR-22 (ADR-0006 audit clause): every `check-fixtures`/`run`/`report` invocation appends a structured entry (actor identity from env/flag, timestamp, purpose, corpus id, verb) to `tools/retro-validate/access-log.jsonl` — an audit trail **distinct** from the review-record chain (no shared files, no shared schema; test-asserted). Append-only (same enforcement pattern as P2-T3's chain layer); log entries carry no case-level data, only corpus-level references. | Each verb appends exactly one entry; seeded entry mutation rejected; a test asserts zero overlap between access-log and review-record file paths/schemas; no case-level data in any log entry (schema-enforced) | 0.5 | general-purpose | sonnet | adaptive | P4-T2 |
| P4-T8 | E0 dangerous-miss corpus promotion adapter (FR-26, PRD OQ-6) | Per FR-26, PRD OQ-6 resolution: implement the deterministic adapter wrapping E0's existing dangerous-miss fixtures (`tests/ef-cbc_suite_v1-dangerous-miss.test.mjs` corpus) in the fixture-corpus envelope (provenance `synthetic`, zero content mutation — the E0 fixtures remain the single source of truth). Adapter output lands in the harness regression lane; a stability test pins adapter output bytes so E0-corpus drift is caught, not absorbed. | Adapter output validates against the P4-T1 schema; byte-level content equality with E0 source cases proven (envelope-only diff); stability test fails on seeded upstream-fixture drift; regression-lane run green through P4-T3's runner | 0.5 | general-purpose | sonnet | adaptive | P4-T3 |
| P4-T9 | Retrospective data-source SPIKE charter (FR-25) | Per FR-25 (ruling R6): author `docs/project_plans/SPIKEs/spike-007-retrospective-data-source.md` — charter only, SPIKE **not run** (running it + any real-data work = gate G3, out of scope, stated in the charter). Contents per FR-25: corpus/partner options (ADR-0006 Option-1 external-partner posture primary), DUA requirements, retention period + deletion trigger (named as the DF-E1-04 spec's must-fix items per ADR-0006), replay-pinning obligations (registry-digest pinning per P4-T3's design), de-identification standard (Safe Harbor / Expert Determination), and explicit success/verdict criteria for the future SPIKE run. Charter frontmatter follows the repo's existing SPIKE conventions (see SPIKE-006). | Charter committed with all 6 content areas; explicit "SPIKE run + real data = G3, not this plan" statement; cross-references gates registry G3 entry and ADR-0006; honesty posture present | 0.75 | spike-writer | sonnet | adaptive | P4-T2 |
| P4-GATE | `task-completion-validator` gate | Verify Phase 4 exit gate: boundary rejections 3/3 + identifier classes ≥6 fail closed; double-run determinism green; 5 metrics present and software-agreement-labeled; protocol thresholds null with non-qualifying banner; access log distinct + append-only; adapter stable; charter committed; `npm run check` green; ADR-delta check (ADR-0006 unchanged, else escalate). | All exit-gate criteria pass; recorded in phase progress note | — | task-completion-validator | sonnet | adaptive | P4-T1..T9 |

**Phase 4 Quality Gates:**
- [ ] `npm run check` green (quality gate: `npm run check` + task-completion-validator)
- [ ] Harness structurally refuses any input not marked synthetic/de-identified; identifier-bearing fixtures 100% rejected
- [ ] Two runs over identical corpus + pinned digest → byte-identical metric artifacts
- [ ] Every metric labeled software-agreement; zero clinical-performance language (grep-proven)
- [ ] Protocol thresholds null / human-only; reports non-qualifying-bannered
- [ ] SPIKE-007 charter committed; access log distinct from review audit trail

[Return to Parent Plan](../evidence-foundry-e1-v1.md)
