# Phase 5: Integration, Honesty Audit & Docs

[Return to Parent Plan](../evidence-foundry-e1-v1.md)

**Column conventions**: `Estimate` in points, never Effort. `Model`: `sonnet` \| `haiku`. `Effort`
(claude only): `adaptive` \| `extended`. Gate rows carry `Estimate: —`.

**Honesty guardrail (every task)**: no output states or implies clinical validity, safety,
diagnostic performance, or regulatory status (FR-28) — this phase's P5-T2 is the audit that proves
it feature-wide. No task clears or depends on gates G0–G4.

---

## Phase 5: Integration, Honesty Audit, Docs & Deferral Closure

**Duration**: ~2-3 engineer-days
**Dependencies**: Phases 2, 3, AND 4 all complete (each phase's validator gate passed)
**Assigned Subagent(s)**: general-purpose executor (**integration owner** — P5-T1 is the R-P3 seam
task joining the three workstreams); documentation-writer (docs/specs; haiku on mechanics);
task-completion-validator gate; **karen feature-end review** (decisions block §2)
**Exit gate** (decisions block §1): end-to-end dry-run green; audit finds zero validity-implying
language; every deferred item has an updated/authored design spec; karen + validator.

| Task ID | Task Name | Description | Acceptance Criteria | Estimate | Subagent(s) | Model | Effort | Dependencies |
|---------|-----------|--------------|----------------------|---------:|--------------|-------|--------|---------------|
| P5-T1 | Cross-workstream integration dry-run (seam task) | R-P3 seam joining P2/P3/P4 outputs: implement `tests/ef-e2e-dryrun.test.mjs` executing the full synthetic chain — (1) the P2-T8 five-role review cycle validates chain-green over the cbc_suite_v1 proposal; (2) `tools/release-sign manifest` + dry-run `sign` + `register` produce a TESTKEY-marked dry-run registry entry whose preimage matches E0 canonical bytes; (3) `tools/retro-validate run` replays the promoted dangerous-miss corpus (P4-T8) pinned to that dry-run registry digest and emits a deterministic software-agreement report. Assert at each hop: every artifact synthetic/dry-run-marked; zero approver fields, zero real signatures, zero `release-ready` transitions anywhere; the discordance→adjudication handoff (P4-T5 ↔ P2 scaffold) round-trips. Integration owner signs off the seam in the phase progress note. | E2E test green in `npm test`; all three hops execute against committed artifacts only; the four inert-state assertions (synthetic marks, empty approvers, no real signature, no release-ready) individually named; adjudication round-trip proven; integration-owner sign-off recorded | 1.25 | general-purpose | sonnet | extended | P2-GATE, P3-GATE, P4-GATE |
| P5-T2 | Honesty-language audit (FR-28) | Per FR-28 (review-blocker), R-P1 bounded target_surfaces — audit exactly these surfaces introduced/changed by this feature: (1) `schemas/review-record.schema.json`, `schemas/reviewer-roster.schema.json`, `schemas/release-registry.schema.json`, `schemas/release-manifest.schema.json` description strings; (2) `tools/review-record/`, `tools/release-sign/`, `tools/retro-validate/` READMEs + CLI help/output strings; (3) the render HTML template + banner copy; (4) `docs/governance/gates-registry.md`; (5) `docs/governance/signing-ceremony-runbook.md`; (6) `docs/project_plans/SPIKEs/spike-007-retrospective-data-source.md`; (7) `agreement-report.json` headers + metric names; (8) committed dry-run artifacts under `modules/cbc_suite_v1/reviews/`; (9) the P5-T3/T4 architecture + CHANGELOG additions. For each surface: flag any language stating or implying clinical validity, safety, diagnostic performance, release-readiness, or regulatory status; verify metrics say software-agreement; verify synthetic artifacts carry non-qualifying labels. Fix findings in place; produce a one-line-per-surface pass/fail checklist for karen (P5-GATE2 input). | 9/9 surfaces audited with written pass/fail lines; zero validity-implying findings remaining after fixes; checklist committed to the phase progress note | 1.0 | general-purpose, documentation-writer | sonnet | adaptive | P5-T1, P5-T3, P5-T4 |
| P5-T3 | `docs/architecture.md` — three new sections (FR-29) | Per FR-29: add concise sections for (a) the review workflow (five-role record chain, roster, append-only + independence enforcement, render surface — pointing to ADR-0004 and the gates registry rather than restating); (b) release signing/registry (canonical-bytes preimage, verify-only CI posture, registry seed, G2 custody boundary per A2); (c) the retrospective harness (boundary, pinning, software-agreement metrics, G3 boundary). Each section restates the unvalidated-research-prototype status and that every human act is an external gate. Existing §1–§10 content otherwise unchanged in substance. | Three sections present, each ≤1 page, each cross-referencing its ADR + gates-registry entry; honesty posture explicit in each; no existing section's substance altered | 0.5 | documentation-writer | sonnet | adaptive | P5-T1 |
| P5-T4 | CHANGELOG `[Unreleased]` entry (FR-29) | Per FR-29, `.claude/specs/changelog-spec.md`: add an `[Unreleased]` entry describing the three new tools, the canonical review-record schema unification, the registry seed, and the SPIKE-007 charter — worded as software machinery ("human-gated", "schema-forced inert", "synthetic dry-run only"), never as clinical capability. Set plan frontmatter `changelog_ref: CHANGELOG.md`. | Entry exists under `[Unreleased]` with correct categorization; wording passes the P5-T2 audit; `changelog_ref` set | 0.25 | changelog-generator | haiku | adaptive | P5-T1 |
| P5-T5 | Gates G0–G4 as external blocked states in progress tracking (FR-27) | Per FR-27 (rulings R2/R4): encode all five gates in `.claude/progress/evidence-foundry-e1/` as externally-blocked owner-action states (status `blocked-external`, owner=human, entry criteria + blocked artifacts copied from the P1-T6 gates registry), mirroring the arc-adoption P5 "owner-blocked" precedent. Gates are never marked as tasks, never completable by agents, and no phase-completion record may claim gate progress. | 5/5 gates present as blocked-external entries with owner=human; zero gate appears as a task row anywhere in progress files; cross-reference to gates registry present | 0.25 | general-purpose | sonnet | adaptive | P5-T1 |
| P5-T6 | Design-spec update — DF-E1-01 (review portal) | Per FR-30: update `docs/project_plans/design-specs/clinical-review-portal-workflow.md` (exists, E0 P7-T3) with E1 learnings: the shipped file+CLI workflow's actual shape (store layout, roster, render), the P2-T8 dry-run friction observations as the first OQ-8 trigger evidence, and the boundary restated — portal promotion is a human decision on demonstrated friction, never pre-emptive. Append path to `deferred_items_spec_refs`. | Spec updated in place (no new file); friction-note findings cited; OQ-8 trigger framing intact; `deferred_items_spec_refs` updated | 0.25 | documentation-writer | sonnet | adaptive | P5-T1 |
| P5-T7 | Design-spec updates — DF-E1-06 + DF-E2-03 (release lane) | Per FR-30: update `docs/project_plans/design-specs/signed-release-key-custody.md` with what E1 actually shipped (sign/verify machinery, exit-code taxonomy, runbook, dry-run posture) vs what stays gated (G0 + G2 promotion, per the A2 reconciliation — custodian distinct from release author, no CI/agent keys); update `docs/project_plans/design-specs/withdraw-rollback-machinery.md` to record the OQ-4 registry seed it will extend (inert withdrawal consts, omitted surveillance hooks) and the E2 boundary. Append both paths to `deferred_items_spec_refs`. | Both specs updated in place; DF-E1-06 spec records the A2 condition; DF-E2-03 spec names the exact inert fields it extends; `deferred_items_spec_refs` updated | 0.25 | documentation-writer | sonnet | adaptive | P5-T1 |
| P5-T8 | Design-spec updates — DF-E1-09 + DF-E2-01/02 (validation lane) | Per FR-30: update `docs/project_plans/design-specs/retrospective-validation-harness.md` for DF-E1-09 (real-data run): harness machinery now exists; remaining scope = G3 (DUA + SPIKE-007 verdict) + human-set protocol thresholds; retention period + deletion trigger named as must-fix per ADR-0006. Update `surveillance-update-registry-engine.md` and `production-monitoring-telemetry.md` with one-paragraph E1-state notes (registry seed exists; nothing deployed/monitored). Append all three paths to `deferred_items_spec_refs`. | Three specs updated in place; DF-E1-09 spec cites SPIKE-007 charter + G3; retention/deletion named; `deferred_items_spec_refs` updated | 0.25 | documentation-writer | sonnet | adaptive | P5-T1 |
| P5-T9 | Design-spec updates — DF-E1-02/03/05/07 + new DF-E1-08 stub | Per FR-30: pointer-refresh updates to `cbc-12-angle-research-operation.md`, `upstream-rf-validators-pediatric.md`, `fhir-terminology-emitters.md`, `property-mutation-semantic-diff-ci.md` (one E1-state paragraph each — unchanged scope, triggers restated, cross-reference this plan). Author the one **new** stub: `docs/project_plans/design-specs/cbc-suite-full-authoring.md` for DF-E1-08 (full CBC Suite ontology / typed facts / local profile contract / rule authoring, §7.3 item 7) with `maturity: shaping`, `prd_ref` to this feature's PRD, `open_questions` naming the OQ-7 rule-schema-v2 trigger reading (orchestrator decision, pending). Append all five paths to `deferred_items_spec_refs`. | 4 specs updated + 1 new stub authored with required frontmatter; DF-E1-08 stub names the OQ-7 dependency; `deferred_items_spec_refs` updated | 0.5 | documentation-writer | sonnet | adaptive | P5-T1 |
| P5-T10 | Frontmatter, findings & DF-EXT-01 closure | Set plan `status` per lifecycle (advance only after P5-GATE2), populate `commit_refs`, confirm `files_affected` matches the actual diff, set `updated`; confirm `deferred_items_spec_refs` lists all 11 spec paths from P5-T6..T9. DF-EXT-01: confirm the E0 consolidated routing note (`.claude/worknotes/evidence-foundry-buildout/rfup-external-routing-note.md`) is still current and record the explicit N/A-with-rationale in the triage table (external routing, no design spec — never counted in `deferred_items_spec_refs`). Findings: if `findings_doc_ref` is null, record "N/A — no findings captured"; else finalize (`status: accepted`, `promoted_to` set). | Frontmatter lifecycle fields complete; exactly 11 entries in `deferred_items_spec_refs`; DF-EXT-01 N/A rationale recorded; findings closed or N/A | 0.25 | documentation-writer | haiku | adaptive | P5-T2..T9 |
| P5-T11 | Full gate re-run + guardrail/non-goal cross-check (karen prep) | Re-run `npm run check` end to end against the final diff; independently re-verify every CLAUDE.md hard guardrail and every PRD §7 non-goal (incl. §6.4 verbatim non-goals: no second crawler, no generative rule writer, no patient LLM path, no guessed LOINC/UCUM, no rf-verify/council-as-clinical-validation, no blended confidence score) against the committed state — one line per guardrail/non-goal, pass/fail, for karen's P5-GATE2 review. Include the PRD §11 seeded-violation checklist status (all 8 classes) and the FR coverage table spot-check. | `npm run check` green; written checklist (guardrails + non-goals + seeded violations, one line each, pass/fail) committed to the phase progress note | 0.25 | general-purpose | sonnet | adaptive | P5-T10 |
| P5-GATE1 | `task-completion-validator` gate | Verify Phase 5 exit gate: E2E dry-run green; 9/9 honesty surfaces pass; architecture + CHANGELOG landed; gates encoded as blocked-external states; 11/11 deferred specs updated/authored + DF-EXT-01 N/A; frontmatter complete; `npm run check` green. | All exit-gate criteria pass; recorded in phase progress note | — | task-completion-validator | sonnet | adaptive | P5-T1..T11 |
| P5-GATE2 | `karen` feature-end review | Decisions block §2's final milestone. Independently verify against the real diff, not descriptions: (1) zero validity-implying language anywhere (P5-T2 checklist re-spot-checked); (2) every forced-empty ceiling intact — `approvedBy[]`, `clinicalApprovers[]`, signature slots, `release-ready` transition; (3) gates G0–G4 modeled as external blocked states, zero gate-as-task rows, no task exit criterion depending on a gate; (4) A1 honored — zero in-repo council artifacts; (5) A2 recorded in the gates registry; (6) no key material, no PHI-capable path, browser posture untouched; (7) deferred triage table fully closed. Plan `status` may advance only after this passes. | karen sign-off recorded; any gap becomes a new task before close-out | — | karen | sonnet | adaptive | P5-GATE1 |

**Phase 5 Quality Gates:**
- [ ] `npm run check` green at feature end (quality gate: `npm run check` + task-completion-validator)
- [ ] Cross-workstream E2E dry-run green; all inert-state assertions pass
- [ ] Honesty audit: 9/9 surfaces pass; zero validity-implying language feature-wide
- [ ] `docs/architecture.md` sections + CHANGELOG `[Unreleased]` entry landed
- [ ] Gates G0–G4 tracked as external blocked states; never tasks
- [ ] 11/11 deferred-item specs updated/authored; `deferred_items_spec_refs` populated; DF-EXT-01 explicit N/A
- [ ] Findings doc finalized or explicitly N/A
- [ ] karen feature-end sign-off recorded — plan may not close without it

[Return to Parent Plan](../evidence-foundry-e1-v1.md)
